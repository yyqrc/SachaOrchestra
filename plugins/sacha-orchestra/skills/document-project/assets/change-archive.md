# {变更标题} — 实施记录

<!-- 生成说明：本模板是候选结构，不是章节清单。优先回答 required_topics；其余章节仅在有实质事实或证据时保留，可合并、改名和重排。短文删除目录；不得留下占位符、空表、空标题、仅写“不适用”的章节或同义重复。生成完成后删除本注释。 -->

**一句话总结**：{用什么机制完成了什么持久变化，以及用户最终能观察到什么。}

<!-- 变更类型只用于选择证据要求，不输出为发布正文元数据。完成程度、影响范围、适用工程或版本和时间只有在影响读者判断时，才自然写入对应正文或修订记录。 -->

## 目录

<!-- 可选：仅在正文较长且目录确实便于查找时保留，并按最终正文重建。 -->

1. 背景与目标
2. 方案选择
3. 实现与数据流
4. 文件改动清单
5. 行为、兼容与影响
6. 专题证据与踩坑
7. 验证结果
8. 限制、回滚与后续

## 1. 背景与目标

{说明原行为、用户需求、成功标准和明确不做的范围。}

## 2. 方案选择

| 候选 | 结论 | 依据与取舍 |
|---|---|---|
| {方案} | {采用/否决} | {原因} |

{写清采用方案成立的前提和不得破坏的不变量。}

## 3. 实现与数据流

```text
{入口} → {关键处理} → {持久化或运行时消费}
```

{以文件 + 类型/方法/字段为锚点说明机制、生命周期和失败语义。}

## 4. 文件改动清单

| 文件 | 操作 | 主要改动 | 同步/生成边界 |
|---|---|---|---|
| `{相对路径}` | {新增/修改/删除} | {内容} | {边界} |

## 5. 行为、兼容与影响

| 场景 | 改动前 | 改动后 | 兼容/风险 |
|---|---|---|---|
| {场景} | {旧行为} | {新行为} | {影响} |

## 6. 关键证据

<!-- 可选：证据较少时合并到“验证结果”。功能、修复、优化只选择一种主要形态；不要为了填模板制造无证据章节。 -->

### 假设、探针或指标（按需）

| 输入/假设 | 探针或指标 | 结果 | 结论 |
|---|---|---|---|
| {项目} | {日志/调试探针/采样方法/单位} | {事实} | {结论} |

### 根因、收益或兼容证据（按需）

| 证据项 | 条件/输入 | 方法或探针 | 预期 | 实际 | 结论 |
|---|---|---|---|---|---|
| {关键证据} | {场景} | {源码/日志/调试探针/采样} | {可证伪预期} | {事实} | {结论} |

## 7. 验证结果

| 验证层 | 输入/场景 | 预期 | 实际结果 | 状态 |
|---|---|---|---|---|
| {静态/编译/运行/设备/性能} | {条件} | {预期} | {实测} | {通过/失败/未验证} |

{记录退出状态、错误、warning 和失败计数；不同证据层不得互相替代。}

## 8. 限制、回滚与后续

- **已知限制与未验证**：{Unknown 或未覆盖场景}
- **回滚点**：{可恢复入口与受影响数据}
- **维护注意**：{必须保持的不变量、同步步骤和关键 path 或 reference}
