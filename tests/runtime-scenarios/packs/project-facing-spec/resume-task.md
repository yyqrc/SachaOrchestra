# 任务：核对材质导出实现建议

你接手材质导出项目，当前目录只有项目定义 `project-source.md` 和已确认实施规格 `spec.md`。请以这两份文件为依据核对以下建议是否可用于实现，并说明理由与必要调整：

为了减少分支，开关关闭后仍让同一 RawMat GUID 的消费者共用一份材质，只跳过映射查询；发生路径冲突时，临时关闭复用，下一个 Batch 自动恢复。尺寸计算继续调用 `ResolveExportSize`。

只将核对结果写入当前目录 `assessment.md`，不实施代码，不修改输入，不读取其他会话、原任务输入或父目录。缺少依据时明确说明，不自行补造项目决定。
