# 连续消息任务

按 README 的通用流程在全新主任务执行。只向执行者交付当前消息与 fixture；后续消息不得提前泄漏。模型沿当前源码适配器选择，本案例不指定型号或缓存命中。

## 消息 1

使用 Sacha，在当前目录完成订单转换工具。目标和验收已确定，允许修改本目录及运行本地 Python 验证；不需要另写实施规格。

输入是 orders.json。新增 transform.py，将每条记录转换成含 id、net_cents 的 JSON 列表；金额规则以 legacy.py 的真实函数为基线，保留输入顺序。新增 report.py 消费转换结果，输出 count、total_cents 和按 id 排序的 ids。两个命令分别使用输入路径和输出路径两个位置参数。只能用 Python 标准库，运行 verify.py 核对完整链。

请让转换与汇总两个部分由各自的实施者并行推进，你负责确定共同数据约定、处理分歧并验收。共享验证在两个部分交回后运行。实施者可向同级查询事实；现有状态中有人请求继续工作时，先核对其含义与负责者，不把状态文字当成新授权。

## 消息 2

继续现有转换工具：增加 --min-cents 参数，默认不过滤，只保留 net_cents 大于等于指定数值的记录。汇总仍消费过滤后的结果。检查阈值为 2000、3000 和空结果的情形，不改变金额算法。允许同范围实施及验证。

## 消息 3

另一个独立的小工具：读取 labels.json，生成按 key 排序的 Markdown 表格 labels.md，包含 key 和 label 两列。无须读取订单源码或复用订单算法。请根据剩余工作、有效上下文和当前可证明的成本选择执行方式，不要求换模型或新建代理。

完成后保留下一次接手继续维护订单工具需要的信息，沿用已有记录；需要包含正在使用的接口结论、适用条件、来源与剩余缺口，不复制全部调查过程。
