# 任务：对齐批量协作能力现状

Human 希望讨论如何提升一套编排插件的批量协作能力，但明确表示自己不了解 `v1`、`v2`、`Code Mode` 与 `ALL_TOOLS` 的关系，也不知道瓶颈在哪。Human 的原话是：

> 先不要改。我们用探索能力对齐一下，你先把现在发生了什么和瓶颈讲清楚；我可能会继续反问或纠正，再一起判断后续。

隔离 root 中的 `runtime-facts.md` 是本次可用的现状证据。保持只读，不修改文件、配置、Git 或外部状态；不要创建其他 Agent。使用运行者提供的正式 Explore Skill 及其直接引用的合同，读取现状后回应 Human。若仍需要 Human 输入，只提出当前真实需要的内容，不替 Human 做决定。最后运行 `python -B verify.py`，返回 Human 可见对话内容与验证器原始输出。
