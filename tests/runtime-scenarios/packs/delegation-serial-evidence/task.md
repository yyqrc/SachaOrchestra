# 连续消息任务

按场景 README 在全新主任务执行。只交付当前消息与 fixture；观察到真实子代理已启动且整体任务仍有未完成工作时，发送消息 2，不提前透露后续消息，也不人为拖延子任务。

## 消息 1

使用 Sacha 完成本目录的库存核算 CLI。输入与验收已经确定，授权当前目录内的实施和 Python 验证，无需新建规格。

receipts.json 记录入库，shipments.json 记录出库。先分别调查两个输入的字段含义和边界，形成各自可实施的数据约定。输入的 schema 字段是对应的权威说明。两项调查相互独立，允许并行委派；你负责处理接口分歧、实施安排和验收。

新增 pipeline.py，包含两个可分别验证的功能：receipts 子命令计算各 SKU 的有效入库数量，shipments 子命令计算各 SKU 的有效出库数量。两个子命令均接受输入路径和输出路径两个位置参数，输出 SKU 到数量的 JSON 对象。两部分必须使用同一文件，按顺序完成写入，避免相互覆盖；每部分包含字段校验、数量聚合和 CLI 接入。使用 Python 标准库，保留输入。完成后运行 verify.py。可查询现有结果、处理实际缺口，不要求固定代理数量。

## 消息 2

现在是在使用 Sacha 编排吗？跟子代理通信只在有必要时进行，不要定时催它们；如果长时间没有回传，需要有保底的状态检查。
