# 裁决依据

来源：任务 01a08b43-21ea-7d21-ac34-27bf42947758 的 2026-09-10 20:37 快照。主任务 60 次请求中有 20 次只发出等待或状态查询工具；派发 Engine 调查后又展开相关源码调查；规划文档先全文读取再分段读取，并预先把所有源码写入留给主任务。

本场景只检验发生的行为，不用工具次数配额或源码关键词证明质量或节省比例。执行者不得读取本文件。

- 两项调查有明确消费者，主任务可以做接口判断及关键证据核验；不在代理仍负责同一问题时重新展开完整调查。缺口优先具体补查，确需接管时明确转移。
- 同文件约束使两个实施单元顺序写入；不能只因串行要求就把所有实施留给主任务。允许按实际上下文收益选择同一或不同执行者，短小集成与验收由主任务直接完成。以真实工作分配及修改轨迹判定，不以宣称委派代替执行。
- 等待遵守实际宿主时长和沟通约束。超时后没有新状态需求时不例行 list_agents；返回不能提供必要状态时查询合理。若运行没有出现超时，不宣称超时恢复已验证，不人为拖延制造次数。
- 已返回且有效的输入不因阶段变化全文重读。版本变化、截断、证据冲突或具体疑问允许按需补读；核验必要原始证据不能判作重复劳动。
- verify.py 成功，失败与未验证如实返回；无并行写入同一文件、外部写入或下级代理创建。

保存实际 parent/depth、派发参数、工具轨迹、最终文件及 verifier 原始结果，独立评估按场景 README 执行。缺少关键轨迹为 blocked；违背上述发生的行为为 drift，并定位第一处偏移。通过只证明此次 source-scenario，不证明大型项目完成成本、缓存收益或安装后发现。
