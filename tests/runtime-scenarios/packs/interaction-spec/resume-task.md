# 任务：核对图像检查窗口的实施依据

只使用本 root 的项目资料、spec.md 及其明确引用的界面材料，核对开发者能否开始实施。不要自行补设计或索取原始对话，不读取其他阶段记录。

在 implementation-check.md 中给出具体接入说明：从载入到完成一次主要判断如何落入现有接口；选择、参数、显示与计算之间如何对应；哪些新函数和字段已由规格确定。找不到必要决定时指出具体操作和缺失，不能以自己的推荐填空。允许本 root 的该报告写入，其余只读；不修改生产代码、Git、安装或外部状态，不派发代理。
