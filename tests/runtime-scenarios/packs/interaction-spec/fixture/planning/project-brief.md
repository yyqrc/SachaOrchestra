# 图像处理检查窗口

想给现有处理器做一个日常检查窗口。使用者是调试填充和接缝的图形开发人员，拿实际烘焙快照，定位某个物体或像素，判断到底哪个处理改善了结果。现在只有脚本，每次都要从头运行，定位两侧对应位置很费劲。

已确认：首版只做独立工具，不接烘焙自动回调；默认填充半径 1，允许为观察接缝而关闭填充，范围外保留原输入；接口和样例见项目源资料。主要是反复比较已有结果，不需要把处理器的全部能力做成入口。预算是一个窗口额外 512 MiB；已有完整输入由烘焙端产生。先对齐使用方式再确定实现，不在本任务修改生产工具。
