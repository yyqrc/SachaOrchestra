# 已批准行为

候选是窗口控制器数据模型。选定填充范围后，Coverage 目标图和目标计数必须对应后续 fill 实际使用的同一目标集合。范围外保留输入。支持区域为索引 2 和 5，半径按绝对索引距离。

选中像素后输入与结果两侧都要显示同一个像素位置；切阶段保留该选择。窗口需要提供 mip0 与 mip1 同时并排比较，不仅是切换级别。验证本控制器即可，当前范围不要求 GPU 或 Unity 运行。

本版不包含滑动幕布比较；未来用户要求加入时属于新需求。禁止通过删改上述要求让候选通过。
