# 任务：为渲染功能形成完整实施 Spec

这是一个全新任务。Human 的请求是：

> 先核对当前公共 C# 数据、原生插件边界和玩家设置，继续澄清会改变方案的决定，然后形成完整实施 Spec。这个 Spec 会作为后续代码修改、构建和运行验收的直接输入；当前只做事实核对和 Spec 对齐，不修改产品代码、不构建、不运行。

隔离 root 中的 `project-facts.md` 已包含判断当前执行方式所需的项目事实；Human 尚未选择是否使用 Sacha。先读取这些事实再回应，不扫描其他项目或加载实施能力。隔离 root 保持只读，不创建 Agent、任务或外部资源。最后运行 `python -B verify.py`，返回 Human 可见回应、实际读取的 Skill/Core path 和验证器原始输出。
