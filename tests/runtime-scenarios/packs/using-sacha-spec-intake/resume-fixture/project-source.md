# 材质导出项目

材质导出器只有 `MeshRenderer` 一个消费者。`MaterialExportSettings` 保存导出宽高，`ResolveExportSize` 读取它并返回最终宽高。输入非法时原有导出器会上报错误且不产生文件；本次不改变此行为。

没有通用渲染器适配器或动态关闭入口。项目需求是同一配置、同一材质得到一致的导出尺寸；不涉及共享材质、数据迁移或运行时资源生命周期。
