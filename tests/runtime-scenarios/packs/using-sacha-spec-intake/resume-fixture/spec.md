# 材质导出尺寸统一

状态：待修订，产品实施未开始。

## 目标

同一配置、同一材质得到一致的导出尺寸。

## 范围与实现

覆盖所有 Renderer，必须新增通用适配器表，为每种 Renderer 提供处理项。导出器通过适配器表取得最终尺寸；保留 `MaterialExportSettings` 和 `ResolveExportSize`。

## 验收

检查适配器表覆盖全部 Renderer，并验证相同输入的尺寸一致。

## 失败保护与回退

必须新增自动恢复状态：失败后停用一次，再重试并恢复；提供运行中开关。
