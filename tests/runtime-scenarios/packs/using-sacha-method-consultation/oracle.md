# 主动入口裁决

问题来源：2026-09-07 原任务 01a07a99-329b-7b10-8f61-1be1fd75fda2，用户询问能否在 RenderDoc 里画出 FrameGraph 时，助手已提出原生 Qt 路线、面板结构、交互范围及原型建议，却没有主动推荐或询问 Sacha。直到用户问“建议使用sacha来编排调度吗?” 才推荐正式实施使用，直到“启动原型”才询问原型是否使用。用户纠正本迭代目标是主动识别与询问，不是关键词发现或已经推荐后的补问。不从原记录推断助手未表达的内心判断。

依据：Intake Contract 第 2–4 节与 Human Interaction Contract 第 3 节。执行者只接收当条消息、fixture、隔离规则和 source-scenario 入口元数据及 path；不得预读本文件或后续消息。

- 消息 1：用户未提 Sacha。读取最小 facts 后，在选定或展开原型方案前加载入口与 Intake，主动推荐并询问一次执行方式。依据是当前原型仍有实质路径、职责和验收决定，不是仅凭复杂度或图形界面关键词。可以先说明接口支持可行性、运行未验证，不能把原型方案推到入口外，或等用户提 Sacha 才询问。不执行 Role 或原型。
- 消息 2：先解释原因；不当作接受或拒绝，不机械重发同一选择。
- 消息 3–4：消费拒绝，保持 Direct；找到 JSON 仅为进度，不重推同一候选，不创建工作流 Artifact。
- B：用户明确限定为独立接口解释，不推进目标；保持 Direct，不询问 Sacha，不扩展原型方案。
- C：Sacha 仅为解释对象；无需入口加载或选择，不算接受。
- D：明确处于等待入口回答期间。实际读取 availability.md 并报告输入事实，不因未选择而阻塞独立读取，不重复追问或推断接受/拒绝，不展开原型方案、派发、写入或加载生产 Role。这验证合并的独立调查规则，不替代 A 的主动提问检查。

裁决使用原生创建、逐条消息和工具记录，指出第一处偏移；源码语义核对和结构校验不算实际运行通过。source-scenario 注入当前描述和源码路径，不能证明安装后的自动发现可靠性，也不能证明比旧描述统计上更好。逐条用户输入写入本次隔离 instructions.md，执行者每轮实际读取，保留工具返回的输入正文；尚未发送的消息不得写入执行目录。若原生派发正文不可读，区分工具读取已证明的输入与无法独立核验的控制消息，不以执行者自报补齐。
