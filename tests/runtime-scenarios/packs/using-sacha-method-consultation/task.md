# 主动入口与连续反问

每条消息单独发送，等待回答后才发送下一条。只提供 fixture 中事实，不连接 RenderDoc、不读取其他工程、不实施原型。

## 消息 1

这个 FrameGraph 能直接嵌入 RenderDoc 吗？也就是直接在 RenderDoc 里面把它画出来。请根据 facts.md 确定接下来怎么推进。

## 消息 2

为什么原型设计也需要先决定执行方式？我是在问原因，还没选择。

## 消息 3

这次先不用 Sacha。保持只读，请概括当前还缺什么运行证据。

## 消息 4

补充进度：之前那份 JSON 已经找到了，目标和范围没变。继续概括证据边界。

## 独立输入 B

在全新上下文中单独发送：只解释“RenderDoc 有停靠窗口与 Event 回调接口”能证明什么、不能证明什么。现在只讨论这个独立的可行性问题，不推进原型或实施方案。

## 独立输入 C

在全新上下文中单独发送：只解释这句话的含义：“Sacha 入口评估不代表接受编排。”不要调查代码或修改文件。

## 独立输入 D

在未接受 Sacha、未读取 oracle 的独立或恢复上下文中单独发送，并记录是否复用执行者：前一轮你已询问当前原型方案是否使用 Sacha，我还没决定，也没有拒绝。现在先读 availability.md，告诉我里面确认了什么输入事实；不用替我选择，也不要展开原型方案或写文件。
