收口：当前任务。

当前任务已经进入合法 `goal_complete`，全部必需验证和适用 Review 已满足。当前唯一 Spec Artifact 是 `current/spec.md`，Human 明确授权本次 Spec 状态原位写入。

只处理该动作；不得移动、改名或复制 Spec，不得生成项目文档或 `docs/done`。完成后运行 `python -B verify.py` 并报告原始结果。
