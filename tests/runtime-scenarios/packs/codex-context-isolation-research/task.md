# 任务：定位单一 CI 故障

使用 Sacha 完成一个自包含调查单元：读取隔离项目 `logs/` 的所有材料，判断构建 1842 首次失败的直接原因、排除的候选和恢复入口。不要修改文件或访问网络。

父任务最终只需要 5～10 条结论，每条带稳定的 `path:line` reference，并列出风险、未知项和阻塞；不需要完整搜索输出、完整日志、长调用栈、完整命令输出或被淘汰方案的推导过程。

