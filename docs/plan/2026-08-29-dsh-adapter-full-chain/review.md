# DSH Single Companion 与 Root Tool Surface 独立复核

> Outcome：`Accepted with follow-up`
>
> 本记录基于 Scope revision 2，取代旧 Revision 1 `Accepted` 裁决。

## Baseline

- Spec：`docs/plan/2026-08-29-dsh-adapter-full-chain/spec.md`
- Execution Report：`docs/plan/2026-08-29-dsh-adapter-full-chain/execution-report.md`
- Git baseline：`0a6c6c687c6af351b049147169b1d866e654636a`
- Runtime evidence：`.temp/runtime-scenarios/2026-08-29-dsh-companion-routing-01/`

## 阻塞问题

无。

## 独立裁决依据

- 未修改 Core、Workflow、Role Skill 或 Codex Adapter；Root policy 与 child bundle 位于 DSH companion，Adapter 只保存传输、恢复和证据映射。
- inspect/execute/review 的首 header 分别为 10/18/9 tools；真实长任务约束没有覆盖主动作分类。
- Agent Teams exact-scope `wait_agent` 可 catalog/help、下一 step 解锁和执行；同一 response 的提前调用被 guard 直接拒绝。
- reset、cold resume、late global catalog、Agent Teams guidance 与 state route 对齐；durable family recovery 使用 committed exact names。
- fresh continuable research child 保持原 allow-list 与 `report`，不含或继承 `sacha_tools`。
- Human 面板只显示自然中文模式、数量和 fallback 行动信息，精确工具名单留在 state route。
- 干净隔离 package 从无 `lib` 状态通过 typecheck、32 tests、build、Preview build 与 35-file pack；无 dirty checkout stale artifact。
- Python 35/35、companion validator、受影响 cprobe 与 Runtime pack 独立评估均通过。

## 非阻塞后续

1. 本轮没有紧邻安装前的 Web Profile package/lock preimage，不能独立证明无关依赖版本未变化。
2. 未获授权的 Desktop Profile 仍指向已删除的旧 Visualizer 路径；迁移前是已知 stale/broken link。
3. family unlock 的 late same-family 不扩权未单独执行完整 DSH Runtime 场景。
4. Linux 与最终非 reduced-motion 动画未验证。

下一路由：允许当前获授权 Web Scope 收尾；以上 follow-up 不阻塞源码与 Web Runtime 交付。
