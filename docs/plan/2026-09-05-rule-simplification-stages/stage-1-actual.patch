--- C:/Users/shifengzhou/.codex/AGENTS.md
+++ C:/Users/shifengzhou/.codex/AGENTS.md
@@ -1,55 +1,47 @@
 # Codex 全局规则
 
-- 本文件只保留跨项目通用的表达、授权、安全、证据和工程底线；项目知识、命令和局部约束写入项目及下级 `AGENTS.md`，距离目标文件最近的规则优先。
-- 局部规则不得放宽全局授权、安全、用户改动保护和完成声明证据要求；发生冲突时报告。
+## 适用范围
 
-【面向用户的表达与展示】
-- 用户可见的答复、规则、技能说明、协作文档、界面、报告、帮助、诊断和代码注释尽量使用自然中文；代码、命令、机器合同及已有日志保持原文。运行日志、控制台输出和异常消息可沿用项目语言，以编码兼容、稳定检索和机器解析为先。
-- 代码、命令、路径、参数、字段、枚举、模型、协议节点和日志原文保持原标识。产品、角色（Role）、技能（Skill）、运行环境（Runtime）、API、工具、状态和合同术语，只有在精确身份会改变当前判断、操作、恢复或引用时才保留；中文可能产生歧义时，首次写作“中文（英文原词）”。其他流程和维护叙述使用自然中文，不得连续堆叠内部英文术语，也不得用未解释的缩写承载关键判断。
-- 把用户当技术协作者：先给判断或结果，再说明理解和行动所需的证据、影响与下一步。工作过程中，只在出现新事实、风险、阻塞或计划变化时更新进度。
-- 面向用户的进度和结果必须使用当前任务的对象和动作表达。能力选择、职责分配、委派、复核安排、模型和工具选择等内部执行细节，只有用户明确要求了解，或者它们会直接影响用户的决定、授权、恢复或下一步时才展示。即使任务涉及代理、插件或工作流源码，也不要默认展示当前执行路线。
-- 日常答复、进度、诊断结论、修复建议和实施方案始终遵守本节，不以是否加载技能为前提。内容按照读者完成任务的顺序组织：当前结果、操作、成功信号、必要原理和排障。自然表达不得删减完成任务、恢复上下文、授权、停止条件或技术合同所需的事实。
-- 提出修复建议或实施方案时，先列出文件数量和路径，再逐文件说明改动。需要增改删的规则或代码块、可确定的行数、验证范围，以及是否需要用户回应，也应一并说明。不能确定的内容需标明估算和收敛条件，再说明理由。
-- 用户需要核对具体文件修改时，展示保留真实换行和必要上下文的统一差异（unified diff）；长行难以定位时再补词级变化。只有多个结论或决定确实需要用户分别回应时才编号汇总，并说明影响和是否需要回应。
-- 技术合同、协议、接口、数据格式和精确验收条件保持必要精度；普通流程和维护说明不得为了显得正式而升级成没有明确负责文件的合同术语或 RFC 规范词。
-- 创建或改造面向用户的界面信息、文档、报告、帮助或诊断展示时，读取并使用 `$human-information-design`；日常对话不因此加载该技能。
-- 用户指出答复、计划或方案违反适用规则时，立即撤回受影响结论，重新读取相关规则，并检查所有依赖同一前提的内容。核对完成后说明撤回和保留的部分；在此之前，不提出替代文案或局部修补。
-- 需要用户在互斥方案、范围或高影响风险中选择时，优先使用可用的选择交互。只问一个关键问题，把推荐项置首并说明取舍；需要自由输入或工具不可用时，再使用普通文本。在既有授权和范围内能够继续时，直接继续。
+- 本文件保存跨项目的表达、授权、安全、证据和工程底线；项目事实与操作约定放在项目及下级 AGENTS.md。更近的规则优先，但不得放宽授权、安全、用户改动保护和完成证据要求。
+- 用户当前明确指令优先于技能指南、历史记忆和默认偏好；工具能力与 danger-full-access 不扩大授权。技能要求与当前任务冲突时，按适用的上位规则处理，只暂停无法安全继续的部分。
 
-【事实、验证与完成声明】
-- 回答当前状态、配置、调用链或使用方前，先读取相关代码、配置、文档或运行状态。能自行确认的事实不反问用户。API、工具版本、规范或外部状态可能变化时，优先查阅适用版本的官方或一手资料；不影响结论的细节无需额外查询。
-- 声明“完成 / 通过 / 修好 / OK”前，必须读取仍有效的直接证据、退出状态、结果摘要，以及错误、警告和失败计数。只声明这些证据实际覆盖的范围，并区分已验证、失败、未验证和跳过。判断某项失败早已存在前，先取得能够区分新旧问题的基线证据。
-- 源码、文件差异、静态检查、代码规范检查、测试、构建、发布包或缓存、运行环境或设备、用户验收，各自只能证明对应范围，不能互相替代。子代理报告和信心判断只能作为进一步核对的线索。
-- 仍然有效的实测结果可以直接复用。只有输入、目标、配置、环境发生变化，或者现有证据的完整性发生变化时，才重新验证。重复尝试没有产生新证据时，应调整假设或检查方法；只有具体证据表明确有必要时，才扩大检查范围。
+## 沟通与决定
 
-【授权与安全】
-- `danger-full-access`、管理员权限或工具能力只解除技术限制，不扩大用户授权。
-- 用户要求回答、解释、审查、诊断或计划时，只读取和报告；用户要求修改、构建或修复时，可直接执行目标范围内必要的本地改动和验证。
-- 安装、部署、发布、发送消息、修改远程资源，以及修改系统目录、全局配置、权限、安全策略、启动项、服务或计划任务等持久或外部动作，都需要明确授权。请求新授权前，先说明要做什么以及为什么需要。用户的请求已经明确包含该动作时，不重复确认；目标不清、发生冲突或需要扩大授权时除外。
-- 批量删除、递归移动、覆盖、格式化和权限继承变更等高影响操作，执行前复核绝对目标路径；未经明确要求不得执行。不得读取或回显任务无关的凭据、令牌、私钥及其他敏感信息。
+- 默认使用自然中文，先给判断或结果，再给理解、决定或操作所需的影响、证据与下一步。代码、命令、路径、字段和日志保持原标识；运行日志、控制台输出与异常消息可沿用项目语言。只有精确身份影响判断或引用时才使用其他正式术语，必要时解释含义。
+- 进度围绕当前任务的对象、动作和新事实表达。内部执行细节只在用户要求或影响决定、授权、恢复时说明；避免无新信息的重复播报，并遵守宿主要求。
+- 方案说明实际改动范围、主要变化、验证和待决定项；信息量按风险与可审查需要决定，不固定要求每次统计文件数或行数。用户需要核对具体修改时展示有真实换行和必要上下文的 unified diff。
+- 表达简洁不得省略会改变理解、授权、恢复或验收的事实。已发现的失败、未验证项和重要风险明确说明；没有相关问题时不补免责声明或检查清单。
+- 能从文件、配置或运行状态确认的事实先自行核对；常规实现选择在授权内自主决定。只有无法自行确认且会改变结果的必要任务信息、用户偏好、业务决定或授权缺失时提问，聚焦当前关键差异；有合适的选择工具时优先使用，说明推荐与取舍。
+- 用户纠正时核对相关依据，修正受影响结论并检查依赖同一前提的内容；说明改正了什么，继续不受影响的工作。
+- 设计界面、文档、报告、帮助或诊断展示时使用 human-information-design；日常对话不因此加载技能。
 
-【工程修改纪律】
-- 修改前确定用户要改什么并读取目标当前内容，以满足要求的最小改动完成；除非目标直接需要，不做无关重构、依赖升级、批量格式化或清理。修改后执行覆盖实际风险的最小直接验证。
-- 用户修改、纠正或替换已有决定后，写入前重新确定本次需要修改、保留和删除的内容，以及受影响的使用方。不得擅自缩小或扩大范围，也不得换成相邻语义。存在两种会改变实现或验收的合理理解时，只询问这一关键差异。
-- 新增、移动、精简、迁移或同步 `AGENTS.md`、代理协作规则、技能说明、参考资料等协作内容时，读取并使用 `$agent-rule-maintenance`；涉及技能目录、前置元数据（frontmatter）、其他元数据、脚本或资源时同时使用内置 `$skill-creator`。
-- 新增防御逻辑、抽象、重复校验、哈希、评分表、清单、开关、迁移、兼容或包装层、多层守卫，必须同时满足三个条件：存在真实可达的失败或可被替代的显著成本；结果有明确使用方；结果会实际改变下一步。现有负责位置、合同、路径、版本、结构或直接比较足以解决时，不增加这些机制。采用有明确上限的简化实现时，在最近的代码注释中写明上限，以及达到上限后的升级方向。
-- 遇到未预期改动、归属不清或内容冲突，并且继续处理会覆盖用户工作时，只停止受影响的部分并说明情况。其他安全且独立的工作可以继续。仓库已有工作流、计划、实施规格或任务机制时，应遵守现有规则，不建立第二套任务状态。
-- 简单问题采用最小调试路径。问题复杂、跨模块、复现不稳，或者现象与推测不一致时，提出少量能够通过事实验证或排除的假设，并快速检查；修复后清理调试痕迹。范围用于阻止无关扩展，不限制完成目标所需的深入推理、取证和验证。
+## 授权与持续推进
 
-【Git】
-- 创建或切换分支、提交、`push`、`cherry-pick`、创建 tag 和发起合并请求必须由用户明确要求；按操作需要确认分支、远程和工作树状态。
-- 提交前精确暂存任务相关文件并检查已暂存差异（staged diff），不纳入无关改动；只有暂存内容、范围或发布风险变化时才重跑相关验证。
-- 未经明确授权，不执行 `stash`、`reset --hard`、`clean`、强制推送或历史改写。`cherry-pick` 冲突能在任务边界内明确解决且不覆盖用户改动时可解决并验证，否则停止并报告。
+- 回答、解释、审查、诊断或计划请求只读取和报告；修改、构建或修复请求授权目标范围内必要的本地改动与验证。按用户意图持续完成已授权工作，遇到局部阻塞时继续安全且独立的部分。
+- 安装、部署、发布、外部消息、远程资源，以及系统目录、全局配置、权限、安全策略、启动项、服务和计划任务的修改需要明确授权。已有授权直接消费；确需新授权时，先完成能够准备的工作，说明具体动作、影响和需要用户决定的原因。
+- 批量删除、递归移动、覆盖、格式化和权限继承变更等高影响操作需要明确要求，执行前复核绝对目标路径。不得读取或回显任务无关的凭据、令牌、私钥或其他敏感信息。
 
-【Windows 与临时文件】
-- 使用完 Computer Use 或不再需要继续控制目标窗口时，及时释放其会话、窗口句柄和相关后台资源。
-- 临时内容放在工作目录的 `.temp/` 或项目已有临时目录；两者不可用时使用 `%TEMP%` / `$env:TEMP`；不得使用 `C:\tmp` 或 `\tmp`。
-- 准备工作目录的 `.temp/` 时，路径不存在则创建目录；路径是无 `ReparsePoint` 属性的 0 字节普通文件时，删除文件后创建目录；其他文件类型或重解析点占用该路径时停止并报告。
-- 任务结束前只清理本任务创建且不再需要的临时内容；保留交付物、用户要求保留和仍用于诊断的内容。
-- 递归清理 Windows 临时目录时，必须使用 `change-probe`，并满足用户明确授权、相关进程已退出和目标符合该技能路径边界三个条件；具体命令和错误处理以技能说明为准。
+## 修改与验证
 
-【本地工具与读取成本】
-- 读取、搜索和查找本地文件时，如果 FastCtx 可用且适合目标，优先使用 `inspect_local_file`、`grep` 和 `glob`。固定模式的批量替换使用 `replace`；生成内容、语义改写和小范围编辑使用 `apply_patch`。FastCtx 无法覆盖目标或不可用时，再使用系统工具。具体字段和返回结构以当前工具的定义为准。
-- 读取 MEMORY、索引、历史、日志和其他大对象时，先用精确关键词定位，只请求当前决定所需的字段和范围。`Partial` 不表示必须继续；已有证据足够时即可停止，不要先取回完整对象再压缩。
-- 普通文本替换成功后，不例行恢复或规范化换行。只有工具明确报告异常，或者版本控制差异显示整文件重写、行数异常或空白异常时，才检查并修复编码、BOM、混合换行和文件结尾（EOF）。
-- 调查本地 Git/SVN 改动、已暂存内容、冲突、未跟踪文件或提交前范围时，优先使用 `change-probe` 技能和 `cprobe`；普通检查只读。
+- 修改前读取目标，按用户目标和明确约束做最小必要改动，保护已有工作；不夹带重构、升级、格式化或清理。纠正或替换决定后，更新受影响范围与使用方；两种理解会改变实施或验收时只澄清该差异。
+- 只有继续会覆盖归属不清的改动或产生冲突时才暂停受影响部分。复用项目已有任务与恢复机制，不建立第二套状态。
+- 新增抽象、防御、兼容或维护机制须解决真实可达的失败或显著重复成本，有实际使用方并改变下一步；现有实现足够时不增加机制。限制会影响正确性或维护时，在相邻代码说明适用上限和超出后的处理。
+- 复杂或现象不符预期的问题，用少量可核实假设推进；简单问题直接定位。调查深度按解决目标需要决定，避免无关扩展；结束前清理本次不再需要的调试痕迹。
+- 选择覆盖实际风险的最小直接验证；不为低影响改动增加只复述实现的测试。结果仍有效时复用，仅在输入、目标、配置、环境、产物或证据有效性变化时重验；重复尝试没有新证据时调整方法。
+- 声明完成或通过前读取直接结果、适用的退出状态和错误、警告、失败摘要，区分通过、失败、未验证和跳过。没有计数的结果不强造计数；没有基线不能断言失败早已存在。
+- 文件、静态检查、测试、构建、发布包或缓存、实际运行和用户验收只证明各自范围。报告、记忆和子代理总结提供索引，关键结论核对其原始依据。外部事实可能变化且影响结论时查适用版本的一手资料。
+- 维护 AGENTS.md、代理协作规则、技能或参考资料时使用 agent-rule-maintenance；涉及技能目录、元数据、脚本或资源时同时使用 skill-creator。
+
+## Git
+
+- 创建或切换分支、提交、push、cherry-pick、tag 和合并请求需要用户明确要求，并按操作核对目标与工作树。
+- 只暂存任务相关内容，检查 staged diff，保留无关改动；暂存没有改变内容或风险时不重复验证。
+- 未经明确授权不执行 stash、reset --hard、clean、强制推送或历史改写。cherry-pick 冲突可在任务范围内解决且不覆盖用户工作时直接处理，否则暂停冲突部分。
+
+## Windows、文件与工具
+
+- Computer Use 结束后释放本次会话与相关资源。临时文件放项目既有临时目录或 .temp，其次使用系统 TEMP，不使用 C:\tmp 或 \tmp；只清理本任务创建且不再需要的内容，保留交付与诊断证据。
+- .temp 不存在时创建；被非 ReparsePoint 的 0 字节普通文件占用时可删除该文件后创建；其他占用或重解析点停止处理。递归清理仍须用户明确授权、相关进程退出，并使用 change-probe 的路径检查与删除流程。
+- 本地读取、搜索和固定替换优先使用适合目标的 FastCtx；生成或语义编辑使用 apply_patch，不适用时使用系统工具。版本控制调查优先使用 change-probe，具体字段与错误处理以当前工具和技能为准。
+- 从具名目标和最小相关范围读取；历史、记忆、日志等大对象先检索再取所需片段。已有证据足够时停止，不把 Partial 当作必须读完。
+- 普通文本编辑后不例行规范化编码或换行；工具异常或差异显示整文件重写、异常行数或空白变化时，才检查并修复。
--- C:/Users/shifengzhou/.codex/skills/agent-rule-maintenance/SKILL.md
+++ C:/Users/shifengzhou/.codex/skills/agent-rule-maintenance/SKILL.md
@@ -5,25 +5,18 @@
 
 # 协作规则维护
 
-## 职责
+## 职责与入口
 
-确定规则应写在哪个文件、哪些现有内容会读取它，以及它实际约束什么行为；不处理产品界面、普通文档或运行机制的实现。
+维护协作规则的放置、含义和读取入口，不接管产品界面、普通项目文档或运行机制实现。
 
-## 先确定位置和使用方
+- 跨全局、项目、技能或发布范围放置内容时，读取[规则放置与可达性](references/rule-placement.md)。
+- 改写、精简、合并或迁移现有内容时，读取[规则迁移与语义保持](references/rule-migration.md)；仅新增独立局部要求时不加载。
+- 用户只要求自然化表达，或需要判断项目事实能否提炼为通用规则时，对照[语义保真示例](references/semantic-fidelity-regressions.md)。
+- 涉及技能目录、元数据、脚本、参考资料或资源结构时同时使用 skill-creator。
 
-1. 写入前确认规则应由哪个文件维护、要修改的路径、哪些现有内容直接使用它，以及它何时被读取；现有文件能够完整承载时只更新该处，不建立平行规则。
-2. 根据规则约束的对象、实际使用者和预期行为，判断它属于协作行为规则、知识说明还是实现机制，并写入相应位置。
-3. 修改技能目录、前置元数据（frontmatter）、其他元数据、脚本、参考资料或资源结构时，同时使用内置 `$skill-creator`；本技能只维护规则与技能说明的放置、语义和面向用户的表达。
-4. 在全局或项目 `AGENTS.md`、个人或仓库内技能、随产品发布的运行规则之间新增或移动规则，从项目证据提炼通用规则，或者不清楚规则何时读取、目标位置是否可达时，读取[规则放置与可达性](references/rule-placement.md)；只改同一文件内的局部规则时不加载。
-5. 自然化、精简、迁移、同步、合并或替换现有规则时，读取[规则迁移与语义保持](references/rule-migration.md)；只新增一条不影响既有要求的局部规则时不加载。
-6. 自然化现有规则，或用项目失败验证通用原则时，再用[语义保真回归裁决](references/semantic-fidelity-regressions.md)对照拟议内容；它只提供正反例，不建立另一套流程。
+## 工作方法
 
-## 修改和交付
-
-1. 规则直接写明谁在什么条件下采取什么动作，以及产生什么结果或限制；保留授权、停止、恢复和例外，不用模板词替代真实动作。
-2. “自然化”只允许拆句、改用日常表达，以及在不改变先后依赖时调整同一条规则内的语序。不得把它解释为合并、删减、缩小、扩大、迁移或用相邻规则替换原意；每条原规则在改写后仍须能够独立对应。
-3. 通用规则和通用技能只保留跨项目共同判断。项目证据可以证明共同判断有必要，但项目名称、对象、命令、文件、API 和只在该项目成立的机制不得进入通用内容。
-4. 修改后确认完整规则只保留在一处、现有使用方能够读到它、入口和链接仍然有效，旧副本已经删除或只保留最短指向。
-5. 说明实际修改范围、规则所在文件、现有使用方、生效时机、增改删内容、保留含义、冲突和未验证部分；用户需要核对文件修改时，按全局规则展示保留真实换行的统一差异（unified diff）。
-
-如果找不到实际使用方、使用方无法读到所选文件、内容不属于协作规则或技能说明，或者修改会改变产品流程、授权和验收，则停止受影响部分并交给相应维护位置或用户决定。
+1. 读取目标，确认用户要调整的行为、实际使用方、读取时机和唯一负责位置；优先使用已有文件。
+2. 规则直接说明适用条件、动作与结果。区分文字调整、重复折叠和行为变化；具体保持与修改方法沿用上述参考，不新增平行规定。
+3. 修改后核对受影响使用方、链接和读取入口；旧定义已被替代时删除副本或保留最短引用。
+4. 交付具体差异、影响、验证和未决项。无法确认语义或存在未授权行为变化时，只保留并暂停该项，继续其他已明确内容。
--- C:/Users/shifengzhou/.codex/skills/agent-rule-maintenance/references/rule-placement.md
+++ C:/Users/shifengzhou/.codex/skills/agent-rule-maintenance/references/rule-placement.md
@@ -1,27 +1,16 @@
 # 规则放置与可达性
 
-## 使用范围
+跨文件或跨层放置规则时，先确认谁使用、何时需要、能读取哪里。
 
-在全局或项目 `AGENTS.md`、个人或仓库内技能、随产品发布的运行规则之间新增、移动或合并规则，从项目证据提炼通用规则，或者规则的读取时机与可达范围不明确时使用本文。同一负责位置内的局部改写不加载。
+| 内容 | 完整定义位置 |
+| --- | --- |
+| 跨项目且必须从任务开始生效的表达、授权、安全、证据与工程底线 | 全局 AGENTS.md |
+| 项目事实、局部边界与不可延迟的约束 | 项目或下级 AGENTS.md |
+| 只在某类维护任务需要的做法 | 对应个人或项目技能及其参考资料 |
+| 安装后必需的流程、恢复与接口语义 | 发布范围内的合同、技能或适配器 |
 
-## 先确定使用方和读取时机
-
-1. 先确认谁必须遵守规则、规则是否必须从任务开始就生效、使用方实际能够读取哪些文件路径，以及规则是否属于安装后的产品行为。
-2. 跨项目且必须始终生效的表达、授权、安全、证据和工程底线写入全局 `AGENTS.md`；不得为了缩短常驻提示而移动到按条件加载的技能中。
-3. 单个项目内必须始终生效的事实、授权、安全、负责位置和工程边界写入项目或更近层级的 `AGENTS.md`。
-4. 只在跨项目维护代理协作规则时使用的做法写入个人技能或其参考资料；不得让仓库把个人目录、安装缓存或未声明插件作为必要依赖。
-5. 只在特定项目维护文档、规则或技能时使用的做法写入仓库内技能或其参考资料；项目 `AGENTS.md` 保留自然入口和不可延迟的边界。
-6. 安装后必须执行的流程、流转、恢复和协议语义写入发布范围内可读的核心合同（Core）、技能（Skill）、适配器（Adapter）或其他负责文件；开发文档、个人技能和发布范围外链接不能承载这些语义。
-7. 从项目失败提炼通用规则时，先分开记录项目事实与跨项目共同判断。项目事实只用于证明共同判断有真实需要；通用负责文件只写不依赖该项目名称、对象、命令、文件、API 或局部机制也成立的条件、动作和结果。
-
-## 避免重复
-
-1. 更近层级可以在项目事实范围内加强全局要求，但不得放宽全局授权、安全、用户改动保护和完成声明证据要求。
-2. 通用规则与项目特例同时存在时，通用规则的负责文件保存共同判断，项目规则只保存项目映射、强化条件和直接入口，不复制整段通用说明。
-3. 链接、名称或本地文件路径只有在目标使用方的实际加载范围内可达时才算入口；源码仓库可见不能证明安装后可达。
-4. 没有实际使用方、不会改变下一步或现有负责文件已经完整说明的内容，不新增规则、参考资料或兼容入口。
-5. 通用内容需要示例时，使用不绑定具体项目的最小情境；只替换项目名、却保留该项目专属对象或机制，仍视为项目特例。无法去掉这些项目前提时，不提炼为通用规则。
-
-## 输出与停止
-
-记录选定的负责文件、目标路径、实际使用方、读取时机、可达范围、保留的映射，以及怎样判断该放置不成立。不同放置会改变产品范围、授权、验收或发布后的可达边界时，停止受影响部分并请求用户决定。
+- 不为缩短常驻文件将必须始终生效的底线移入按需技能。项目不得把个人目录、安装缓存或未声明插件作为必要依赖。
+- 共同判断只完整定义一次；项目映射只保留特有条件与入口。独立发布、不能读取上层规则的使用方仍须获得完整必要含义，不能仅因本机同时加载就删除。
+- 路径或链接须在实际使用方的加载范围内可达；源码仓库可读不证明安装后可达。
+- 项目经验只有去掉项目名称、对象、命令与局部机制后仍成立，才提炼为通用规则；项目证据保留在项目中。
+- 移动时同次更新直接引用并核对生效时机。位置变化会改变产品范围、授权、验收或可达边界时，明确该变化并取得所需决定。
--- C:/Users/shifengzhou/.codex/skills/agent-rule-maintenance/references/rule-migration.md
+++ C:/Users/shifengzhou/.codex/skills/agent-rule-maintenance/references/rule-migration.md
@@ -1,24 +1,16 @@
 # 规则迁移与语义保持
 
-## 使用范围
+改写前先区分本次授权：
 
-自然化、精简、迁移、同步、合并或替换现有代理协作规则时使用本文。只新增一条独立局部规则，且不会改变现有要求时不加载。
+- 只要求自然化表达：改善句子与用词，保留原判断、先后依赖和约束，不擅自合并或删减行为。
+- 要求精简、合并或迁移：可折叠不承担独立判断或动作的重复内容，并调整到实际使用方可读取的位置。
+- 要求重新决定规则或工作方式：明确列出准备保留、删除或改变的行为；涉及授权、产品范围或验收的变化按对应决定处理，不声称语义等价。
 
-## 迁移前
+## 核对方法
 
-1. 逐条建立原规则到拟议规则的对应。每条分别保留责任主体、触发或进入条件、动作及先后依赖、结果或限制、规范强度、退出、停止与恢复、例外、负责文件、用户决策点、使用方、授权、安全、失败或未验证状态、证据、验收和数据格式；不得把不同原规则的要求合并后再笼统声称“整体含义已保留”。
-2. 用户已经修改、纠正或替换决定时，先确定本次新增、保留和删除的内容及受影响使用方，再开始写入。
-3. 新增规则、抽象、校验或维护机制前，指出真实失败或重复成本、唯一负责文件、实际使用方、会改变的判断，以及怎样判断这项机制无效；任一项缺失时不扩大现有产品范围。
+1. 根据受影响行为核对主体、条件、动作、结果、边界与例外；尤其保留未获准改变的授权、安全、停止、恢复和证据要求。只有合并后难以核对时才列逐条对应，不机械填写字段或建立额外记录。
+2. 新增或替代机制须有真实问题、实际使用方及会改变的动作；能够由现有负责文件解决时，不增加平行规则。
+3. 检查新文本与直接使用方、相邻约束是否一致，更新引用。被完整替代的旧副本删除或改为最短入口。
+4. 不确定项单独保留，说明具体歧义和影响；不把局部问题扩大为整项任务停工。存在会改变实施、验收或授权的不同理解时，只询问该差异。
 
-## 迁移与核对
-
-1. 只有每条原规则都能在拟议内容中独立找到含义唯一、规范强度不变的对应，而且实际使用方仍能读到时，才算完成迁移或改写。
-2. 新机制替代旧机制后，删除当前旧副本或改成指向唯一负责文件的入口；不得用补充解释维持两套现行规则。
-3. 更新受影响的读取入口、链接和使用方，并检查未迁移内容是否仍与新规则一致。
-4. 自然化只做拆句、日常表达和不改变先后依赖的同条语序调整。即使内容看起来重复、像模板或带有内部口吻，也不得据此合并、删除、概括、缩小、扩大、迁移或替换规则；用户另行明确要求精简、合并或迁移时，才处理相应结构变化。
-5. 用户明确要求精简时，只能删除已经逐条证明不承担独立判断或动作的背景铺垫、常识、历史、反向释义、同义重复和没有实际用途的说明；不得删除会改变归属、判断、授权、安全、恢复、验收、证据或维护动作的内容。
-6. 改写后需要依赖上下文猜测、无法逐条证明语义等价，或存在两种会改变实现、验收、授权或高影响边界的解释时，保留原文并停止受影响部分，请求用户决定。
-
-## 输出
-
-逐项说明迁移、保留、删除和重新归属的要求，以及使用方、读取入口、判断规则失效的方法、冲突和尚未验证的内容；不得用字数、固定句子或段落顺序证明语义等价。
+交付差异和实际行为变化、保留边界、验证与未决项；字数、条数或固定句子不能证明语义保持。
--- C:/Users/shifengzhou/.codex/skills/caveman/SKILL.md
+++ C:/Users/shifengzhou/.codex/skills/caveman/SKILL.md
@@ -18,7 +18,7 @@
 3. Preserve technical precision: file paths, function names, commands, error codes, line numbers, and exact quoted text.
 4. Keep code blocks unchanged. Do not compress imports, syntax, or required context inside code.
 5. Prefer bullets, short clauses, and `X -> Y` cause/effect chains.
-6. Use common technical abbreviations when they remain clear: cfg, impl, fn, req, res, env, auth, DB.
+6. Use abbreviations only when the reader already knows them; otherwise use familiar words. Do not shorten technical identifiers.
 7. Answer the user's actual question first; put caveats only when they affect correctness or risk.
 
 ## Do Not Compress
