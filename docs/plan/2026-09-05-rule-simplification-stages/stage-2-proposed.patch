--- AGENTS.md
+++ AGENTS.md
@@ -2,13 +2,12 @@
 
 > 文档身份：插件开发使用；不进入发布插件。
 
-## 工作区事实
+## 工作区与发布边界
 
-- 本文件是项目级 `AGENTS.md`；全局规则中的安全、授权、证据、Git 和用户改动保护仍然生效。
-- 本文件、根目录 `README.md`、`PLUGIN_DESIGN.md`、`EVOLUTION.md`、`.agents/skills/**` 与 `docs/**` 供插件开发使用，不进入发布插件。
-- 本仓库的本地 Agent Plugin 市场只发布 `plugins/sacha-orchestra`；单独构建和安装的唯一 DSH 配套包位于 `integrations/dsh/sacha-companion`，不进入三个市场，也不属于 Agent Plugin 的发布 `root`。
-- 当前发布版本、待发布源码版本、破坏性变更边界、成熟度和尚未实施的长期方向以 [`EVOLUTION.md`](EVOLUTION.md) 为准；现行架构和流程仍由 `PLUGIN_DESIGN.md` 及相应的运行时负责文件定义。部署清单记录当前源码版本，Git 标签标记已经发布的版本。
-- `EVOLUTION.md` 只记录版本、当前破坏性变更边界、成熟度和尚未实施的方向，不授权实施。
+- 本仓库只发布 `plugins/sacha-orchestra`，三个部署清单均以它为发布 `root`。独立构建、安装的 DSH 配套包在 `integrations/dsh/sacha-companion`，不进入三个市场或主插件发布范围。
+- 根 AGENTS.md、README.md、PLUGIN_DESIGN.md、EVOLUTION.md、.agents/skills/**、docs/** 与 tests/** 仅供开发维护使用；发布插件的运行语义与本地引用必须在发布 root 内完整可达。开发决定需要安装后使用时，写入插件内相应负责文件，不能以开发文档链接替代。
+- 当前源码版本、发布版本、破坏性边界、成熟度和长期方向读取 EVOLUTION.md；部署清单记录源码身份，Git 标签标记已发布版本。EVOLUTION.md 不定义当前流程、不授权实施、不保存版本流水账。
+- 全局授权、安全、用户改动保护和证据底线持续生效。以 GPT-6 Astra 为主要开发使用对象，按实际风险选择方法，避免重复确认、提前停止和无收益机制；不据模型能力放宽产品边界。
 
 ## 负责文件与直接入口
 
@@ -34,102 +33,49 @@
 | `integrations/dsh/sacha-companion` | 唯一 DSH 配套包：负责 Profile 内 Root 工具暴露的任务分类、限制、查询、解锁与恢复，组合 continuable child surface，并拥有 Host/Client 状态投影、构建和界面；只消费现有 Sacha 任务、角色和已提交事实，不定义入口、决策关口、就绪判定、授权、调度、评审结果、完成判断或发布插件运行语义 |
 | `plugins/sacha-orchestra/skills/*` | 不绑定具体运行环境的节点职责、局部工作流与边界；不增加流程节点，不复制核心合同判断或适配器参数 |
 
-## 文档身份与发布可达边界
+## 按任务读取
 
-- 文档作者新增或修改用户可读文档前，必须先根据路径确定它供“插件开发使用”还是“插件发布使用”；一份文档只能有一种身份。
-- 仓库根 `AGENTS.md`、`README.md`、`PLUGIN_DESIGN.md`、`.agents/skills/**`、`docs/**` 与 `tests/**` 内的说明只供插件开发、维护或场景评估使用，不进入发布插件，也不得成为安装后的依赖。
-- `plugins/sacha-orchestra/**` 内的用户可读文档随插件发布，必须在发布 `root` 内完整提供安装后所需的规则、入口和恢复语义。
-- 三个部署清单都把 `plugins/sacha-orchestra` 解析为发布 `root`，因此只有该目录内的文件会随插件发布。在源码仓库中能够读取文件、打开链接或解析相对路径，并不能证明发布 `root` 外的文件在安装后仍然可达。
-- 发布插件内的本地引用必须解析到发布 `root` 内，不能依赖根 `AGENTS.md`、根 `README.md`、`PLUGIN_DESIGN.md`、`docs/**`、`tests/**` 或其他发布 `root` 外文件承载运行语义。
-- 如果安装后的运行环境需要使用插件开发文档中的决定，修改者必须把该决定写入插件内相应的核心合同、技能或适配器负责文件，并同步会使用它的内容；不得用指向插件开发文档的链接代替插件内定义。
-- 评审者（Reviewer）声明发布文档自包含前，必须根据部署清单确认发布 `root`，检查本次新增或修改的本地引用都在该 `root` 内可达，并运行插件校验。该校验只能证明所覆盖的源码和静态结构；安装后是否可达，还需要安装证据或发布包一致性证据。
+- 从目标路径、文档用途和实际使用方开始，读取当前负责文件与必要的直接引用，不预加载整个设计、历史或所有运行环境。
+- 处理 docs/** 前读 docs/AGENTS.md；涉及共享术语时先查 docs/CONTEXT.md 的相关项，再核对插件内定义。讨论入口、节点、连线或角色职责时读 PLUGIN_DESIGN.md 及受影响合同、技能；仅查局部触发时读目标技能和元数据。
+- 单一运行环境只读目标适配器，涉及共同判断时才跨到核心合同。继续既有迭代时先读当前适配器、批准规格和本次差异；具名场景与历史证据只在恢复或当前验证确需时读取。
+- 只有发布、版本、破坏性边界或长期方向需要时读 EVOLUTION.md；发布或安装的操作入口见本文件末节。不能用旧规格或历史报告覆盖当前定义。
+- 维护规则与文档使用 sacha-doc-governance；具体插件差异评审使用 sacha-plugin-review；简化调查使用 sacha-simplification-audit；实际场景运行或评估使用 sacha-runtime-scenario。它们只用于插件开发，不成为安装后依赖。
 
-## 需要读取哪些文件
+## 开发改动与决定
 
-开发者或评审者讨论、调查或修改文档时，按以下规则选择读取范围：
+- 普通修改、构建、修复、同步和迭代在当前任务直接推进。范围由用户目标和明确约束决定；预计文件列表仅在用户或批准 Spec 明确规定时才是硬边界。同范围的必要源码、文档、测试与配置可一并处理，缺陷与补证由当前任务继续完成。
+- 只有需要用户重新决定的范围、实质方案、破坏性合同或数据格式、权限、安全、持久数据含义、验收或外部动作发生变化时，才暂停依赖该决定的写入。先完成可安全准备的事实和具体方案，已有批准不重复索取。普通开发不因规则文件、工作量或证据缺口而自动接受 Sacha、打开产品 Gate；已明确使用 Sacha 时仍按其现行合同处理。
+- 验证缺口按本次交付要求处理：必需证据不足时不作对应完成声明，保留恢复条件；非阻塞项如实说明，其他已授权工作继续。其他运行环境未验证不自动阻塞当前源码交付，也不自动新增后续任务。
+- 新增或改变入口、节点、连线、角色或技能职责、输出类型、负责位置与特殊流程，先取得用户对具体变化的批准，再按 `需求与不变量 → PLUGIN_DESIGN.md → 工作流或相应核心合同 → 技能与适配器使用方` 修改。已有批准直接消费；只改变职责内做法、读取新事实或表达时，修改对应负责文件，不为同步改顶层设计。
+- 默认使用现有通用流程。新增特殊路线须先说明真实失败、现有流程的缺口、拟新增节点或连线及影响；不增加隐藏旁路、第二套状态或生命周期。新增 Hook、MCP、应用、外部服务或权限字段仍需明确批准；Hook 不接受或替代 Sacha，不扩大授权，也不参与恢复。
+- 新规则、抽象、Artifact、字段、模板或校验器须解决真实问题或显著重复成本，有唯一负责位置、实际使用方和会改变的判断，并能说明何时无效；优先补强现有内容。只记录支撑当前决定的事实，不强制另做表单或重复确认。交付前核对这些依据与相邻边界仍成立；依据变化时修正受影响内容，不能为了“更完整”扩张产品。
+- 脆弱且反复执行的机械步骤使用脚本并验证真实入口；需要用户或环境选择的稳定参数放入现有配置，无需调整的实现常量保留在代码。不要仅因步骤多就新增包装层或配置。
+- 项目接入、领域知识和特定命令归使用项目或领域技能；核心合同不收项目特例。目标项目的方案与交付物应在不依赖 Sacha 内部角色、Gate 或 Handoff 叙述时仍可理解和执行。
 
-- 先根据目标路径、文档身份和实际使用方选择主要工作范围。根 `AGENTS.md`、根 `README.md`、`PLUGIN_DESIGN.md`、`EVOLUTION.md`、`docs/**` 与 `tests/**` 属于插件开发文档，优先检查开发行为、维护归属、读取路线和开发证据；`plugins/sacha-orchestra/README.md`、`core/**`、`adapters/**` 与 `skills/**` 随插件发布，优先检查安装后的流程、负责文件、实际使用方和行为证据。
-- 同一任务同时涉及开发文档和发布插件时，必须先分别说明开发决定以及需要同步到发布插件的内容；只有实际使用方、发布可达性或真实行为验证需要时才跨越两边。不得用开发文档对自身的描述评判发布插件文档，也不得用安装后的局部流程扩大插件开发文档的范围。
-- 新增、移动、审查或维护 `docs/**` 前，必须先读 `docs/AGENTS.md`，再根据目标文档的身份只读当前负责文件或确切相关的历史工作记录；不得遍历没有实际用途的文档树。
-- 任务涉及已提炼术语或规则时，必须先读 `docs/CONTEXT.md`，再核对其中列出的插件内定义。发布插件不得引用 `docs/CONTEXT.md`。
-- 讨论或修改入口、高层流程、角色或技能职责时，必须在读完适用的术语定义后读取根 `PLUGIN_DESIGN.md`，再读取受影响节点对应的入口、工作流、用户交互、保障、协调、工作记录合同或目标技能。只查询局部触发条件或流程且不涉及已提炼术语时，只需读取目标 `SKILL.md` 和元数据。
-- 使用 `.agents/skills/*` 时，必须先按该技能规定的范围读取本文件和现行负责文件，并只把它作为插件开发工作流；开发技能不得承载安装后的运行语义，也不得扩大产品技能的职责。
-- 单一运行环境的局部任务只读目标适配器；只有任务确实涉及核心合同或多个运行环境时，才在任务范围内比较相应内容。
-- 在新任务或协作界面继续同一运行环境的局部迭代时，必须先读目标适配器、已批准实施规格、专用运行场景和本次有界改动。只有这些现行来源不足以恢复状态时才查询 MEMORY 或历史会话摘要；完整旧对话、完整 MEMORY 和冻结历史计划都不是默认输入。
-- 处理发布、`1.0.0`、尚未实施的长期方向或核心合同的破坏性变更边界时，必须读取 `EVOLUTION.md`。显式快速发版、普通发版或安装还要读取 `docs/release.md`；处理现行架构、Manager、并行或产品流程时，必须先读 `PLUGIN_DESIGN.md`。只有改变 `EVOLUTION.md` 独占的内容时才修改它。
-- `EVOLUTION.md` 只用于读取当前发布版本、待发布源码版本、破坏性变更边界、成熟度和尚未实施的长期方向。历史事实必须按具名版本、实施规格、报告、评审或 Git 查询，不从 `EVOLUTION.md` 恢复旧机制。
-- 普通实施不遍历历史计划目录寻找额外规则。现行内容与当前负责文件冲突时，删除副本或改成指向负责文件的引用；冻结历史文档只有在仍会误导当前操作时，才增加最短的“已取代”入口。
-- 项目接入（Project Integration）和领域技能（Domain Skill）归各自项目所有；不得把项目特定命令或证据规则导入此核心合同。
+## 内容归属与同步
 
-## 普通插件开发（Direct）
+- 负责位置以本文件入口表为准；每项事实只完整定义一次，使用方保留引用与自身映射。顶层设计保存完整骨架，工作流合同决定流转，其他核心合同决定各自判断，技能描述职责内做法，适配器映射宿主参数。
+- Manager 闭环在顶层设计画完整；工作流决定进入和返回，协调合同决定评估、依赖、就绪与派发，Manager 技能调用，适配器组装参数。删除越权副本，不靠补充解释维持重复。
+- Codex 型号、强度、创建参数和回退只在 Codex 适配器维护；其他现行文档不复制型号表。参数说明由负责文件和实际使用方核对，行为声明按下节取证，不做逐型号或参数组合的文字测试。
+- 角色技能保留职责、流程和禁止边界；控制或支持技能只能实现顶层设计已声明的功能，主流程外技能说明功能与副作用。局部迭代是否越过既有职责，按上节判断。
+- 被多个发布插件使用的共享术语在插件内术语合同定义，docs/CONTEXT.md 保存引用、使用方和核验方法；只供多个开发文档使用的术语由 CONTEXT.md 定义。单任务决定留在本任务记录，不为对称新增字段、状态或术语分类。
+- 发现词义或定义冲突时先核对当前事实，能够消除歧义的在授权内统一；会改变产品范围、验收或授权的取舍才交用户决定。提炼只命名已有判断，动作仍由相应合同定义。
+- 共享术语的含义或边界改变时，同次更新术语合同、负责判断的合同和实际映射；名称、位置或使用方变化时更新 CONTEXT.md 索引。开发专用术语开始被多个发布插件使用时，先提升到插件内定义，再将开发侧改为引用。未对齐前不声明该项完成。
+- base、root、path、reference 沿用相应定义，不混用或新增 locator 别名。根 README 只提供仓库导航，插件 README 提供入口、最小用法和运行时导航；setup 只管理相应配置。
+- 替代现行机制时删除旧副本或改为最短引用；历史工作记录只在仍会误导时加最短“已取代”入口，不重写冻结正文。
 
-- 普通的插件修改、构建、修复、同步或迭代请求默认在当前任务直接完成；不得仅因为文件多、耗时长或验证步骤多而单独打开决策关口。
-- 直接处理范围（Direct Scope）由用户的语义目标和明确约束决定。预计会修改的文件不是完整白名单；只有用户或已批准实施规格明确给出精确文件白名单时，文件列表才构成硬边界。
-- 修改者为完成同一目标，可以修改直接必需的源码、文档、断言和验证配置。在同一范围内发现漏改或任何失败时，直接修复并重新验证，不创建额外流程。
-- 如果工作中出现实质新方案、破坏性合同或数据格式变化、权限或安全变化、持久数据变化、验收变化、未授权外部动作，或者无法完整验证，必须停止受影响的写入并按三个决策关口处理。
-- 每次只验证当前运行环境和交付层。除非用户明确要求跨运行环境验收，否则其他运行环境在安装、发现、行为或证据上的缺口只作为后续反馈，不扩大或阻塞本次源码发布。
-- 评审只拦截会导致错误交付的关键问题。不会阻塞发布的证据缺口或改进项使用 `Accepted with follow-up`；不追求覆盖全部历史或所有运行环境，也不把“还可以补充证据”升级为 `Needs Fix`。
+## 工具与验证
 
-## 术语提炼与同步
+- 使用生成器或创建工具前只检查当前动作需要的入口和依赖，按当前 plugin-creator、skill-creator 或对应脚本说明操作，不硬编码用户路径。新建技能、生成元数据和更新市场使用当前创建工具；市场和缓存写入须有明确授权，不手工修改已注册状态。
+- 依据本次差异选择最小充分验证：
 
-- 修改者发现同词多义、同义多名、概念误合并，或者代码、合同与文档定义冲突时，必须先只读核对当前负责文件、实际使用方、真实行为和冲突内容。事实足以消除歧义时，在当前授权和范围内直接统一；只有不同解释会改变产品范围、验收、授权或破坏性边界时，才交给用户决定。该调查采用直接处理（Direct），不打开 Explore、Planner Gate 或 Manager Gate。
-- 同一术语或流程判断被多个发布插件使用、已经产生歧义或造成真实失败时，修改者必须在插件内术语合同中提炼该术语，并在 `docs/CONTEXT.md` 保存定义链接、使用方和核验方法。术语合同只定义含义与边界，具体进入条件和动作仍由相应核心合同定义，其他发布插件只保留自身映射。
-- 只有被多份开发文档共同使用的术语才由 `docs/CONTEXT.md` 负责，不得为了让两边集合一致而把它写入术语合同。没有第二个现行使用方时，当前任务语义必须留在实施规格或决定记录；只有存在跨任务的项目使用方时，才能列为 `project-context` 候选。提炼术语只能命名已有判断；如果要改变判断，必须按新增或扩展规则处理。没有机器使用方和实际判断需要时，不得把术语升级为字段、状态、枚举、工作记录（Artifact）、必填格式或产品行为。
-- 修改者新增、改名、改变语义或边界，或者删除插件内共享术语与规则时，必须在同一次修改中更新插件内术语合同、相应核心合同和受影响映射；名称、位置或使用方变化时同时更新 `docs/CONTEXT.md` 的引用。词义以术语合同为准，流程以相应核心合同为准。
-- 修改者处理只供开发文档使用的术语时，只更新 `docs/CONTEXT.md` 和开发侧使用方；如果该术语开始被多个发布插件使用，必须先把定义提升到术语合同，并将开发侧定义改为引用、更新受影响使用方。更新完成后才能使用受影响术语，本次修改也只有到这一步才算完成。
-
-## 内容归属与信息密度（必须遵守）
-
-### 内容放在哪里
-
-- 核心合同、适配器、技能和开发文档都必须遵守全局规则中的【面向用户的表达与展示】；编写、迁移、精简或审查这些文本时使用仓库内 `$sacha-doc-governance`，不得以正式标识为由保留无关的内部话术。
-- 每项事实只能由一处完整定义，以下归属均为硬性边界：项目事实写在本文件；完整顶层设计写在 `PLUGIN_DESIGN.md`；被多个发布插件共同使用且不属于单一运行环境的提炼术语写在插件内术语合同；只被多份开发文档共同使用的提炼术语写在 `docs/CONTEXT.md`；流程判断写在相应核心合同；角色内部流程写在技能中；单一运行环境的传输、模型和恢复写在适配器中。
-- `docs/CONTEXT.md` 保存插件内共享术语的引用、使用方和核验方法，并完整定义开发专用术语；`PLUGIN_DESIGN.md` 必须引用它。插件内其他内容只能引用插件内负责文件，或者保留自身需要的映射。
-- `PLUGIN_DESIGN.md` 是插件开发和评审使用的唯一顶层设计文件，完整保存流程骨架和角色、技能职责；它与根 `AGENTS.md` 都只供插件开发使用，不进入发布插件，也不能成为安装后的依赖。完整设计只能由插件开发者、评审者和场景评估者读取。工作流合同（Workflow Contract）唯一且完整地定义安装后的流程，并沿用插件内术语合同；其他核心合同只定义各自的局部判断，技能只保存自身职责、流程和边界，适配器只负责传输；这些内容不得要求使用方读取或复制完整设计。
-- Manager 协调闭环按层次分工：`PLUGIN_DESIGN.md` 必须画出完整闭环；工作流合同只决定何时进入闭环以及返回哪个调用节点；协调合同（Coordination）定义评估、依赖、就绪判定、派发、等待和返回；Manager 技能调用这些规则；适配器组装运行参数。某一层越权时，必须删除越权副本，不得再补充解释维持重复。
-- Codex 适配器唯一负责自动模型组合、选择条件、`agent_type/model/reasoning_effort/fork_turns` 和回退。顶层设计、核心合同、技能、README 和通用历史说明不得复制当前型号表。Markdown 中的映射必须由负责文件及实际使用方复核；需要对实际行为作验收结论时按本文件“按改动选择验证”的触发条件运行对应真实场景，不为覆盖所有型号和参数另造场景，不得用正则或固定标记测试锁定文字；将来如果改为机器可读配置，必须测试真实配置使用方，而不是测试说明文本。
-- 开发文档和发布插件使用 `base`、`root`、`path` 或 `reference` 时，必须分别沿用 `docs/CONTEXT.md` 中的开发定义和插件内术语合同中的安装后定义。修改已有内容时必须消除混用，不得再创造 `locator` 作为同义术语。
-- 显式设置（setup）必须只管理对应配置；根 `README.md` 只保留仓库导航，`plugins/sacha-orchestra/README.md` 只保留入口、最小用法和运行时负责文件导航，历史记录和版本迁移说明必须写入具名文档。适配器、技能、顶层设计和运行流程的归属沿用本文件的“负责文件与直接入口”表。
-- Planner、Executor 和 Reviewer 三个角色技能必须分别写清自己的职责、工作流和边界。修改这些技能前，修改者必须先确认变化仍在对应角色已经声明的输入、输出和禁止边界内；如果要新增职责、输出类型、负责位置转移或跨节点路线，必须先按高层流程变化处理，不得直接给技能增加一步；为既有职责读取新的事实来源本身不构成职责变化。
-- Manager、Explore、Feedback、using-sacha、document-project 等控制或支持技能必须只实现 `PLUGIN_DESIGN.md` 中对应的节点或闭环。roadmap、setup-project、setup-agents 等主流程外技能必须写清功能、概略工作流和副作用边界。迭代必须只修改已声明功能内的做法；新增功能、触发方式、外部副作用或跨节点接管时，必须按顶层设计变化处理，不得混入局部修复。
-
-### 修改顺序与产品边界
-
-- 用户明确确认高层流程的节点、先后关系、分支、角色或技能职责、回路变化后，必须按 `需求不变量 → PLUGIN_DESIGN.md → 工作流或相应核心合同 → 节点技能或适配器使用方 → EVOLUTION.md（若改变长期或破坏性变更边界）` 的顺序修改。只改变节点内部判断或职责内流程而不影响顶层设计时，必须直接修改唯一负责该内容的核心合同、适配器或技能，不得为了“保持同步”修改设计文档。
-- 新机制替代旧机制后，必须删除现行内容中的旧副本并引用当前负责文件。冻结历史文档必须只在仍会误导当前操作时保留最短的“已取代”入口；`EVOLUTION.md` 不得新增历史表格行或版本章节。
-- 新增或扩展规则、角色、决策关口、工作记录（Artifact）、状态、字段、模板或校验器前，修改者必须指出真实失败或重复低效、唯一负责文件、实际使用方、会改变的判断，以及怎样判断该机制无效，并优先补强能够解决问题的现有负责文件；缺少任一条件都不能增加，“更完整”或“更规范”也不能作为扩大产品范围的理由，示例、标签和局部做法不得自动升级为核心合同或必填格式。同步实际使用方后，交付前必须再次确认上述条件、上位或相邻规则的约束强度以及产品边界仍然成立；条件不成立时先修正并重新检查，无法证明符合时不得交付受影响内容，最终结果必须说明检查结论和尚未验证的内容。
-- 默认路线必须复用工作流合同（Workflow Contract）定义的通用运行流程。需要提速时，必须关闭没有事实依据的决策关口、跳过不成立的候选，并避免加载没有实际用途的文件；不得增加特殊任务、目标限制、隐藏旁路或第二套生命周期。确实需要特殊流程时，必须先向用户说明真实失败模式、通用流程为什么不足、拟新增的节点和连线、负责文件及其影响；取得明确批准后，由维护者先修改 `PLUGIN_DESIGN.md`。
-
-### 实现选择与主流程独立性
-
-- 多种做法都成立时，必须写清选择原则；稳定参数必须写入配置；脆弱且会重复执行的机械顺序必须写成脚本并实际运行。
-- 主流程必须在脱离 Sacha、固定决策关口、确定范围和交接信息（Handoff）后仍能完成；编排只能增强协调、恢复或独立验收，不能成为主流程成立的前提。
-
-## 产品边界
-
-- 产品入口、生产角色、支持或控制技能、主流程外能力及其完整职责清单只由 `PLUGIN_DESIGN.md` 第 2、4、5 节负责。当前改动不涉及入口、节点、连线、职责或负责位置转移时，不复制或重新读取这份完整清单。
-- 新增角色、技能功能、节点、连线、结果（Outcome）去向或跨节点负责位置转移前，必须先向用户说明产品范围变化并取得明确确认；确认后先修改 `PLUGIN_DESIGN.md`，再修改核心合同和实际使用方。现有职责内的流程、提示词或证据细节不得自动升级为顶层设计变化。
-- 核心合同只保存多个流程节点共同使用、且不属于单一运行环境的稳定判断。单个技能的触发条件、内部流程、局部状态或格式留在技能中；单一运行环境的传输、模型和恢复留在适配器中；项目特例留在项目接入或领域技能中。找不到第二个实际使用方时，不能新增核心合同分类或必填字段。
-- Hook 不得接受或替代 Sacha，不得扩大授权，也不得参与恢复。新增 Hook、MCP、应用、外部服务或权限字段必须取得明确批准；完成目标直接必需的仓库本地资源、脚本或部署清单元数据，可以在当前范围内修改并验证。
-
-| 变化类型 | 修改与核查范围 |
+| 改动 | 核对与验证 |
 | --- | --- |
-| 高层流程节点、连线、角色或技能职责、负责位置转移 | 取得用户明确确认后，先修改 `PLUGIN_DESIGN.md`，再修改工作流或相应核心合同、节点技能、受影响适配器和 `EVOLUTION.md` 中的边界；需要实际行为验收时按本文件触发条件使用真实场景证据，不增加说明文字测试 |
-| 核心合同中的节点判断 | 修改唯一负责该判断的核心合同，再检查直接调用或返回的技能以及受影响适配器；顶层设计没有变化时不修改 `PLUGIN_DESIGN.md` |
-| 角色或支持技能的局部流程 | 修改必须留在已声明的职责或功能内，并同步元数据以及直接调用或返回方；超出边界时按高层流程变化处理 |
-| 单一运行环境的传输、模型、安装或恢复 | 只修改目标适配器、相关元数据或部署清单；需要实际行为验收时按本节触发条件运行真实环境验证，不联动其他运行环境或核心合同 |
-| 设置、生成器、Provider catalog 或 Skill loading | 修改具体技能和生产脚本，检查能力提供方指南，并测试真实生成行为；不把生成格式提升为核心流程 |
-| 产品版本、待发布源码版本或发布状态 | 修改 `EVOLUTION.md`、三个部署清单和 Git 标签；发布校验器只检查机器可解析的部署身份和可执行入口 |
-
-## 创建工具与生成器
-
-- 运行生成器前，必须先找到当前 `plugin-creator`、`skill-creator` 和能够导入 PyYAML 的 Python；不得硬编码用户路径。
-- 新建技能使用 `init_skill.py`；生成元数据使用 `generate_openai_yaml.py`，并提供 `display_name`、25～64 字符的 `short_description`，以及包含 `$skill-name` 的 `default_prompt`。
-- 更新市场必须另行授权，并使用当前创建工具；不得手工修改已经注册的市场或缓存。
-
-## 按改动选择验证
-
-修改者完成普通插件改动后，必须根据实际改动选择最小且充分的验证。机械检查用于确认结构、数据和生产入口没有被破坏；真实运行只在有具体行为问题、明确高风险后果或用户要求时执行。以下命令不是默认全部执行的清单，报告结果时只说明每项验证直接覆盖的范围：
+| 开发文档、核心合同、适配器或技能纯正文 | 核对负责位置、实际使用方、语义、链接和有界差异；运行 cprobe，不因文件属于插件就运行结构校验器 |
+| 技能前置元数据或 agents/openai.yaml | 对受影响技能运行 quick_validate.py；不以结构结果代替语义 |
+| 部署清单、技能目录、代理元数据、MCP/应用、资源或打包路径 | 运行 validate_plugin.py；声明自包含时另核对发布 root 与新增或修改的引用 |
+| 生产脚本、解析器、生成器或机器数据接口 | 在隔离临时目录调用真实入口的最窄测试，验证可解析状态；按实际风险覆盖成功、失败、文件副作用、幂等、恢复和禁止行为；横跨完整测试面时才全量运行 |
+| 触发、流转、派发/返回或宿主调用语义 | 修复已观察的实际行为问题、高风险后果不宜等待日常暴露，或用户明确要求运行验收时，运行对应真实场景；其他情况按源码和使用方核对 |
 
 ```powershell
 python -B -m unittest discover -s tests -p 'test_*.py'
@@ -138,22 +84,14 @@
 cprobe summary <affected-path-or-directory> --json
 ```
 
-- 如果只修改核心合同、适配器、技能正文或开发文档，并且没有改变前置元数据、其他元数据、资源、打包路径、部署清单、机器合同和生产脚本，修改者必须检查唯一负责文件、实际使用方、链接和文件差异，并对受影响范围运行 `cprobe`。不得仅因文件位于技能或插件内就运行技能或插件校验器。
-- 如果修改技能的前置元数据或 `agents/openai.yaml`，必须对受影响技能运行 `quick_validate.py`。该结果只证明技能和元数据的结构正确；正文语义和实际行为仍需分别验证。
-- 如果修改插件部署清单、技能目录结构、代理元数据、MCP 或应用、资源或打包路径，必须运行 `validate_plugin.py`。该结果只证明插件能够被读取且结构正确；纯正文改动不得运行该命令。
-- 如果修改生产脚本、生成器、解析器或机器可读的数据格式或使用方，必须运行能够覆盖受影响入口的最窄测试；只有改动横跨完整测试面时才运行全量单元测试。测试结果不得外推到没有执行的实际运行行为。
-- 如果修改技能触发、角色流转、派发或返回、运行环境调用语义，先判断是否需要真实运行：本次修改用于修复已经观察到的实际行为问题；改动涉及权限、安全、删除或覆盖状态、不可逆外部动作等不宜等待日常使用暴露的高风险后果；或用户明确要求实际运行验收。只有满足其中一项才必须运行对应真实场景。其他规则或提示文字改动按负责文件、实际使用方和有界差异检查，不因“行为尚未实跑”自动增加后续验证任务。
-- Python 默认由 Codex 全局 `shell_environment_policy` 注入 `PYTHONUTF8=1`；生产脚本仍显式使用 `encoding="utf-8"`。不得给每条命令机械添加 `-X utf8`；只有实测 `sys.flags.utf8_mode != 1` 或出现解码错误时，才对受影响命令使用这一后备方式。
-- 当 `cprobe` 返回 `budget.complete=true` 且 `whitespace.errors=0` 时，该结果已经提供当前范围的空白字符证据，不再重复执行 `git diff --check`。只有 `cprobe` 缺失、结果不完整或不支持目标时，才对同一范围执行一次原生 Git 后备检查；暂存后内容没有变化时也不重复取证。
-- 测试或校验器不得通过读取顶层设计、README、核心合同、适配器或 `SKILL.md`，再检查正则、固定标记、整句存在或缺失、段落顺序或字数来证明语义。源码校验器必须只检查 JSON、TOML、YAML 等机器可解析的部署身份、实际文件结构、可执行入口和 Git 发布身份；Markdown 链接和语义由负责文件及实际使用方检查，不得增加上述文本关卡。只有生成 Markdown 本身是生产输出时，才能通过真实消费者、产物或原字节核对；纯文本匹配仅保留极少数实际参与安全解析的所有权或模板控制标记，且必须同时观察解析结果与副作用。普通标题、词句和错误文案不作为断言目标，不建立关键词白名单。机器数据字段、命令参数和错误码按实际接口验证，不借此证明角色流转或运行行为。
-- 测试生产脚本时，必须调用真实入口，并在隔离临时目录中覆盖正反例、可解析状态、文件或真实副作用、幂等、失败恢复和禁止行为。需要实际行为验收时，技能触发、角色流转、派发与返回以及运行环境调用按本节的真实运行触发条件运行对应场景；生产脚本测试不得代替真实运行场景。
-- 角色或流程场景必须有明确具体案例及可观察的失败或成功结果，先确认现有案例不能覆盖；不得因流程变化、型号、角色、参数或状态枚举而展开全组合。保留的包也不是每次必跑的清单。场景只能按照 [`tests/runtime-scenarios/README.md`](tests/runtime-scenarios/README.md) 中的任务包、执行者与评估者隔离及原生证据流程运行。静态源码、校验器、测试夹具、执行者自报和未安装的 `source-scenario` 都不得代替对应的真实运行证据。
-- 只有实际运行生产入口，才能声称对应行为已经验证。模板、测试夹具、字符串或自报只能证明其自身；未触发实际运行验收时，不需要把没有运行的行为列成待补验证。
+- 上述命令是选择入口，不是必跑清单。实际场景的新增条件、隔离、证据与独立评估只按 [场景说明](tests/runtime-scenarios/README.md) 执行；保留场景不等于每次运行，不展开角色、型号或状态矩阵。
+- 不用 Markdown 的关键词、固定句子、标题顺序或字数证明规则语义。生成 Markdown 是生产输出时，可通过真实消费者或产物字节核对；少量实际参与安全解析的所有权/模板控制标记须同时观察解析结果与副作用。机器字段、参数和错误码按真实接口验证。
+- 完成判断只覆盖本次交付与已有证据。源码、结构、测试、包/缓存、实际运行和人工验收分别陈述；只有运行生产入口才能声称行为已验证。未触发实际运行要求时，不自动把没有运行的行为列成待补任务。
+- cprobe 完整且 whitespace.errors=0 后不重复 diff --check；缺失、不完整或不支持目标时才对同范围使用原生 Git 后备。Python 通常已有 PYTHONUTF8=1，生产脚本仍显式 UTF-8；实测未启用或出现解码错误时才为受影响命令加 -X utf8。
+- plugin-eval 不是必跑关卡，不代替结构校验或行为证据；不为分数增加字段、英文触发词或规则。输入不兼容时可在当前任务使用等价镜像并说明限制，不改正式源码或安装缓存迁就工具。
 
-`plugin-eval` 可用于检查结构、描述和令牌预算，但不是必跑关卡，也不得替代官方校验器、真实数据格式、代码测试或运行环境冒烟验证。不得只为提高评分而增加没有权威依据的部署清单字段、英文触发词、引用或其他产品内容。如果评估器输入不兼容，必须在当前任务内使用等价镜像并说明限制；不得修改安装缓存或正式源码来迁就工具。
+## 评审与发布
 
-## 发布与安装入口
-
-- 用户明确要求“快速发版”“发版”、安装、重装或缓存一致性验收时，执行者必须先读 [`docs/release.md`](docs/release.md)，再按其中的当前模式、授权和证据边界操作。普通实施批准不包含提交、标签、推送、市场或用户安装状态变更的授权。
-- 发布和安装仍要遵守全局规则中关于 Git、外部副作用、用户改动保护和完成证据的要求。操作指南和 `scripts/release.py` 不能代替用户的版本决定、评审、安装授权或运行环境验收。
-- 没有进入显式发布或安装任务时，不加载发版操作指南，也不得因为源码版本、仓库本地直连或插件已启用就执行安装、刷新或缓存修改。
\ No newline at end of file
+- 评审只拦截会导致错误交付的关键缺陷；非阻塞事项使用 Accepted with follow-up。没有会改变结论的具体缺口时结束，不追查全部历史、运行环境或模型分支；有效原始证据可复用。
+- 只有明确要求快速发版、发版、安装、重装或缓存一致性验收时，读取 docs/release.md 与相关 EVOLUTION.md 内容，按当前模式执行。普通实施不包含提交、标签、推送、市场或安装授权，也不自行刷新缓存。
+- 本文件和操作脚本不替代用户的版本、发布或安装决定。只有版本、待发布范围或破坏性边界变化时才修改 EVOLUTION.md 和相应部署身份；Git 标签仅在已授权发布时操作。
--- docs/AGENTS.md
+++ docs/AGENTS.md
@@ -1,25 +1,17 @@
 # Sacha Orchestra 开发文档规则
 
-> 文档身份：插件开发使用；不进入发布插件。
+> 本目录只供插件开发使用，不进入发布插件；发布可达和变更纪律沿用[根规则](../AGENTS.md)。
 
-## 身份与负责文件
+## 内容位置
 
-- `CONTEXT.md` 是开发文档侧提炼术语与规则的统一入口，并负责开发专用术语；它保存插件内共享术语的定义链接、使用方和核验方法，但不复制入口、角色、批准、迁移或协调动作。
-- `integrations/**` 保存当前能力提供方、项目接入和其他开发接入指南；指南只解释开发者如何提供、维护和核查接入，不成为安装后的依赖。
-- `release.md` 保存用户明确要求快速发版、普通发版或安装时使用的开发期操作指南；当前版本身份仍由 [`EVOLUTION.md`](../EVOLUTION.md) 负责，机械执行仍由 [`scripts/release.py`](../scripts/release.py) 负责。
-- `plan/**` 保存具名任务的实施规格（Spec）、实施报告（Execution Report）和评审记录（Review Artifact）；它们只服务对应任务、恢复或历史取证，不定义当前产品流程、安装后流转或版本状态。
+- CONTEXT.md 定义开发专用共享术语，并索引插件内术语的负责文件、使用方和核验方法；不复制运行流程。
+- integrations/** 解释能力提供方、项目绑定和开发接入，按当前任务只读具名指南及实际使用方。
+- release.md 保存显式发布或安装的操作步骤；版本身份由 EVOLUTION.md 负责，机械动作由 scripts/release.py 执行。
+- plan/** 保存具名任务的决定、实施规格、实施报告与评审记录；仅供该任务和恢复取证使用，状态或“已批准”字样不能替代当前定义与本次授权。
 
 ## 读取与维护
 
-- 修改提炼术语或开发规则时，开发者或评审者先读 `CONTEXT.md`，再按[根 `AGENTS.md`](../AGENTS.md) 核对 [`PLUGIN_DESIGN.md`](../PLUGIN_DESIGN.md)、插件内负责文件和实际使用方；不得从历史任务工作记录恢复当前机制。
-- 修改能力提供方、绑定或项目接入时，开发者或评审者只读 `integrations/` 中的目标指南及其具名负责文件和使用方；不得遍历无关提供方或历史计划。
-- 用户明确要求快速发版、普通发版或安装时，发布执行者读取 `release.md`、[`EVOLUTION.md`](../EVOLUTION.md) 和精确的已暂存差异；不得为了恢复背景读取全部开发文档或运行时负责文件。
-- 当前任务精确引用 `plan/**` 中的工作记录时，只读该路径、当前使用方和必要原始证据。工作记录的状态、日期或“已批准”措辞不使其成为当前产品权威；普通实施不得扫描 `plan/**` 寻找规则或“最新”任务。
-- 历史工作记录与当前负责文件冲突且仍会误导当前操作时，维护者只增加最短的“历史或已取代、替代版本或当前负责文件”入口；不改写冻结正文，也不建立第二套生命周期或集中状态索引。
-
-## 内容与验证
-
-- 开发文档可以链接发布插件内的运行时负责文件；发布插件文档不得依赖本目录承载安装后语义。
-- 当前事实、开发决定、运行环境映射、任务工作记录和测试输入分别留在唯一负责文件中；移动或删除文档时同次修复直接入口和可达链接，不复制正文维持旧路径。
-- `tests/runtime-scenarios/**` 的 `task.md`、测试夹具和 `oracle.md` 是测试输入和裁决材料，不是产品文档；场景流程由 [`tests/runtime-scenarios/README.md`](../tests/runtime-scenarios/README.md) 负责。
-- 纯开发文档改动复核身份、负责文件、实际使用方、链接和有界差异，并按[根 `AGENTS.md`](../AGENTS.md) 选择最小验证；不得用文字正则或运行环境校验器证明文档语义。
+- 术语问题查询 CONTEXT.md 的相关项；只有入口、角色或高层流程确实相关时才读取 PLUGIN_DESIGN.md，其他任务按根规则查直接负责文件。
+- 历史记录只在当前任务明确引用或当前来源不足以恢复时读取；不扫描计划目录寻找新规则，不改写冻结正文。仍会误导时加最短的历史说明及当前入口。
+- 文档之间使用可达引用，同次修复移动或删除导致的直接链接；验证按根规则选择，不建立额外文档关卡。
+- tests/runtime-scenarios 的任务、夹具和 oracle 属于测试输入与裁决材料，其执行流程见[场景说明](../tests/runtime-scenarios/README.md)。
--- .agents/skills/sacha-doc-governance/SKILL.md
+++ .agents/skills/sacha-doc-governance/SKILL.md
@@ -5,37 +5,20 @@
 
 # Sacha 文档治理
 
-## 功能
+在明确范围内整理规则、技能与文档，确认读者、负责位置和实际可达性；不改变未获批准的产品行为。
 
-在明确范围内判断每份文本供谁使用、内容应写在哪里、哪些现有内容依赖它，以及安装后能否读取；精简文字时保留所有会改变判断或行动的含义，并把历史材料与当前有效内容分开。
+## 开始
 
-## 先确定范围和用途
+- 先从用户目标、路径或差异确定范围；能从上下文明确的自行确定，只有歧义会改变产物或范围时才提问。
+- 读取[根规则](../../../AGENTS.md)中的负责位置与读取入口；docs 内容再读 docs/AGENTS.md，术语查 CONTEXT.md 相关项，入口或职责变化查 PLUGIN_DESIGN.md。技能或适配器正文使用[文本参考](references/skill-and-adapter-text.md)。
 
-1. 要求用户给出目标路径、差异或明确子树；范围不清时停止，不推导全仓审计。
-2. 先读[项目规则](../../../AGENTS.md)。目标位于 `docs/**` 时再读[开发文档规则](../../../docs/AGENTS.md)和适用的 [`CONTEXT.md`](../../../docs/CONTEXT.md) 条目；目标位于 `plugins/sacha-orchestra/**` 时读取定义相关安装后行为的文件及其直接引用方。
-3. 只有入口、节点、连线、角色或技能职责、内容归属可能变化时读取 [`PLUGIN_DESIGN.md`](../../../PLUGIN_DESIGN.md)；普通局部文本不借此扩大范围。
-4. 目标是技能或适配器文本时，读取[技能与适配器文本](references/skill-and-adapter-text.md)；其他文档不加载该参考说明。
+## 整理与核对
 
-## 整理和核对
+1. 按用途区分开发文档、发布内容、任务记录和测试输入；根据实际使用方确认唯一负责位置及读取时机。安装后必需内容须在发布 root 内可达。
+2. 面向读者用自然中文，保留影响理解、授权、恢复和验收的事实及精确技术身份。代码与机器合同不靠翻译或关键词判断，表达原则沿用全局规则。
+3. 区分纯表达、重复折叠和行为调整。核对受影响要求的条件、动作、依赖、结果和例外；难以核对的合并才逐条列对应，不机械枚举所有字段。未经批准的产品、授权或验收变化不作为文案修改写入。
+4. 删除不承担独立判断的重复解释；完整定义留在负责文件，使用方保留引用与自身映射。核对直接相邻内容，不扩成全语料清理。
+5. 将依赖会话、临时编号或不可达草稿的现行叙述改为有来源的当前事实；历史记录按根规则处理。生成内容修改其来源，不手写第二份生成状态或验证清单。
+6. 复核实际使用方、链接与剩余歧义，按根规则选择最小验证。无法确认的内容保留并说明，其他已明确且获授权的部分继续。
 
-1. 先按路径判定文本用于插件开发、随插件发布、记录任务还是提供测试输入；一份文档只保留一种用途。随插件发布的本地引用必须位于部署清单解析出的发布 `root` 内。
-2. 根据谁会使用内容，区分用户可读叙述、正式标识、代码与机器合同。面向用户的流程和维护叙述使用自然中文，不得连续堆叠内部英文术语或用未解释缩写承载关键判断；产品、角色（Role）、技能（Skill）、运行环境（Runtime）、API、字段、枚举、状态和已经明确归属的术语按原标识保留，中文可能产生歧义时首次写作“中文（英文原词）”。不得用关键词扫描代替语义判断。
-3. 确定每项事实由哪个文件完整定义，以及哪些现有内容会使用它。现有文件能够承载时只修改该处，其他位置改为最短指向或链接，不新建平行说明。
-4. 改写前逐项列出需要保留的主体、触发或进入条件、动作及先后依赖、结果或限制、规范强度、退出、停止与恢复、例外、负责文件、用户决策点、使用方、授权、安全、失败或未验证状态、证据、验收、进入条件（Entry Condition）和数据格式（schema）；只有这些事实全部保留且含义唯一时才压缩。
-5. 只保留会改变内容归属、使用方、流程判断、授权、安全、恢复、验收、证据或维护动作的限制；删除背景铺垫、常识、历史、反向释义、同义重复和没有实际用途的说明。
-6. 遇到依赖会话视角、评审过程、临时计划编号、不可达草稿引用或控制流程的叙述时，检查仓库读者能否在不依赖对话、PR 线程或未提交草稿的情况下解析引用并验证事实。不能时把真实事实改写为当前状态并删除过程叙述；没有事实的过程文本直接删除。
-7. 不把现行机制的历史、生成结果或测试清单手写进负责定义机制的文件。生成内容应修改其来源；源码、发布包或缓存、实际运行和用户验收分别报告，不互相替代。
-8. 移动、删除或标记取代前核对引用关系和完整取代关系。历史工作记录（Artifact）只在仍会误导当前操作时增加最短的“历史或已取代、当前负责文件”入口，不改写冻结正文。
-9. 复核相邻文本是否依赖同一语义；只处理实际使用方，不扩成没有明确用途的语料库清理。
-
-## 输出
-
-- 报告实际检查或修改范围、文档用途、完整定义所在文件、现有使用方、完成的迁移或精简、刻意保留的内容、冲突和未验证部分。
-- 用户只要求审查时只报告发现；获授权的修改使用真实统一差异（unified diff），并运行适合目标文件的最小验证。
-
-## 何时停止
-
-- 文本变化会改变产品入口、流程、角色或技能职责、授权、验收或破坏性边界时停止写入并交用户确认，不把语义变化伪装成文案整理。
-- 改写后需要依赖上下文猜测、存在会改变判断的多种合理解释或无法证明语义等价时保留原文；不能用补充解释掩盖不确定语义。
-- 不用关键词、固定句子、段落顺序或字数证明语义；不因“更完整”增加规则、工作记录（Artifact）、字段或校验器。
-- 本技能不执行项目文档生成、实际运行场景、发布、安装、Git 写入或外部动作。
+只读请求报告发现；修改请求交付实际差异、行为影响、验证与未决项。产品范围变化按根规则取得对应决定，已有批准不重复索取；本技能不执行发布、安装、Git、运行场景或项目文档生成。
--- .agents/skills/sacha-plugin-review/SKILL.md
+++ .agents/skills/sacha-plugin-review/SKILL.md
@@ -5,32 +5,23 @@
 
 # Sacha 插件评审
 
-## 功能
+只读审查明确差异，找出会导致错误交付的产品、实现、发布可达或证据问题；不修复、不发布，也不替代运行时独立评审。
 
-审查一个明确的 Sacha 插件差异，找出会导致错误交付的产品行为、运行时、安装内容、自包含或验证问题，并把阻塞问题与后续改进分开。
+## 输入
 
-## 输入与首查
+- 使用用户指定的 commit、range、工作区或暂存差异建立基线；远端只在比较对象或发布范围确实涉及它时核对。输入变化时更新受影响范围，不重新索取未改变的批准。
+- 按[根规则](../../../AGENTS.md)读取当前负责文件和直接使用方；涉及用户可读规则或文档时使用[文档治理](../sacha-doc-governance/SKILL.md)。历史仅在会改变结论的具体缺口需要时查询。
 
-1. 要求可解析的提交、范围、差异、文件集或指定的工作区范围；核对当前远端基线与目标提交，目标变化后重新确认评审范围。
-2. 先读[项目规则](../../../AGENTS.md)，再按目标 path 区分开发控制面和发布 Runtime。差异包含用户可读文本时完整读取 [Sacha 文档治理](../sacha-doc-governance/SKILL.md)，消费其语言与完整命题检查；涉及提炼术语时读 [`docs/CONTEXT.md`](../../../docs/CONTEXT.md)，涉及入口、流程或职责时读 [`PLUGIN_DESIGN.md`](../../../PLUGIN_DESIGN.md)。
-3. 读取差异、定义相关行为的相邻文件、实际使用方和真实生产入口；范围报告、校验器和测试结果只提供线索，不替代语义评审。
+## 检查
 
-## 动作顺序
+1. 核对差异是否在既有职责内；实际改变入口、节点、职责或负责位置时，检查批准与设计到使用方的同步，不因读取新事实就认定越权。
+2. 对改变的接口追踪声明、实现和实际消费者，按影响核对失败、取消、所有权、清理、恢复和持久化。新增公共能力须有真实使用方与产品依据。
+3. 涉及限制、拒绝或授权时，追到最终副作用及可能绕过的真实入口；对模型或用户可见内容检查实际提示、数据结构、结果和诊断，不以关键词判断语义。
+4. 发布内容核对部署 root 与引用；纯开发内容不无条件检查安装。验证应命中实际入口，代码测试、结构、包与运行证据分别判断；关键禁止行为需要对应真实入口的负例。
+5. 复用仍有效的原始证据，只补会改变结论的缺口。按根规则的触发条件判断是否需要运行场景，未触发项目不自动成为后续验证任务。
 
-1. 检查改动是否写在现有 Core、Skill、Adapter、开发文档、测试或生成器负责的文件内；新增入口、职责、跨节点路线或职责转移必须先取得用户批准并自上而下同步。
-2. 对变更接口追踪声明、实现和当前消费者，核对成功、失败、取消、所有权、清理、恢复和持久化边界；只有单一内部消费者的公共扩张必须有产品依据。
-3. 对限制、拒绝和授权路径追到最终副作用，检查直接调用、替代入口、包装层和监听顺序能否绕过。对模型或用户可见变化读取实际提示词、schema、结果、诊断和受影响模式。
-4. 核对发布 root、manifest 路由和本地引用；开发文档可引用 Runtime Owner，发布文档不得依赖 root 外文件。源码、包/缓存、安装后发现和 Runtime 行为分别裁定。
-5. 检查验证是否命中真实加载入口、生成器、Adapter、工具、进程入口或场景；聚焦测试、构建、校验器、Runtime 和用户验收不互相替代。断言应在目标回归出现时失败，关键禁止行为需要真实入口负例。
-6. 只在问题可能改变交付判断时扩大路径或证据引用。已经由自动检查准确覆盖且没有语义缺口的事项，不重复列为人工问题。
+## 结果
 
-## 输出
-
-- 每个问题给出紧凑位置、缺陷、影响和直接证据；区分阻塞交付的问题与 `Accepted with follow-up` 候选。
-- 报告准确的评审范围、复用和重新执行的证据，以及尚未验证的运行时或安装行为；不得把实现者自检当作独立结论。
-
-## 停止与禁止边界
-
-- 本 Skill 只读，不修复、提交、发版、安装、创建任务或修改外部资源。
-- 没有会改变交付判断的具体问题时停止，不追查全部历史、所有 Runtime 或无人使用的定义。
-- Runtime Reviewer 的 Baseline、Outcome 和返回路由仍由发布插件的 Assurance Contract 与 Reviewer Skill 拥有；本 Skill 不建立第二套裁决合同。
+- 每个问题说明准确位置、影响和直接证据；关键缺陷阻塞，非阻塞项可 Accepted with follow-up。覆盖范围与限制如实说明，不将实现者自检当作独立结论。
+- 必需检查已能形成判断、没有具体的新证据缺口时结束。运行时 Reviewer 的 Baseline、Outcome 与返回仍由发布插件的相应合同负责，不建立第二套流程。
+- 不修改交付文件、提交、安装、创建任务或操作外部资源。
--- .agents/skills/sacha-simplification-audit/SKILL.md
+++ .agents/skills/sacha-simplification-audit/SKILL.md
@@ -5,31 +5,16 @@
 
 # Sacha 简化审计
 
-## 功能
+只读查找明确范围内重复、已被替代或成本过高的机制，区分可折叠的副本与需要重新决定的产品能力。
 
-从明确范围内找出少量有直接证据的简化项，区分可以直接清理的重复实现与需要用户重新决定的产品能力。
+## 调查
 
-## 输入与首查
+1. 从具名路径、机制或横切目标确定范围；能从当前任务确认的自行确定，缺少会改变范围的信息时才问。按[根规则](../../../AGENTS.md)读取当前定义、实际使用方和必要入口，不默认扫描全仓或历史。
+2. 核对生产、发布、测试与文档的直接使用方及加载来源；名称、零搜索命中和静态工具提示不足以证明无人使用。只有当前来源不能解释替代关系时查询具名历史。
+3. 重点检查重复判断、未被消费的字段或模板、无收益的 Hash/包装层，以及旧机制是否已被完整替代；只处理会改变当前决定的项目，不为了对称或形式增加机制。
+4. 对候选说明真实成本、保留的行为、删除后失去的能力、实际使用方与最小验证。复杂度只转移到另一个包装或文件不算精简。
+5. 有生产使用、持久格式或兼容义务时，说明需要用户决定的影响；无法证明可替代时保留。认定完整替代前核对仍有效的代码、接口、文档和测试，不以保留测试本身反推生产需求。
 
-1. 要求一个明确子系统、路径、机制或用户指定的横切主题；范围不清时停止，不默认扫描全仓或历史任务目录。
-2. 先读[项目规则](../../../AGENTS.md)，再读取当前定义、实际使用方和真实发布或 Runtime 路径。涉及产品入口、Role/Skill 职责时读取 [`PLUGIN_DESIGN.md`](../../../PLUGIN_DESIGN.md)，涉及术语时读取 [`docs/CONTEXT.md`](../../../docs/CONTEXT.md)。
-3. 只有当前定义无法解释机制或准确的替代关系时，才查询历史 Artifact 和 rollout；不把旧方案当作当前依据。
+## 交付
 
-## 动作顺序
-
-1. 为每个候选列出生产或发布使用方、测试或文档使用方，以及尚未确认的使用方；读取调用点、加载路径和生成来源，不能只凭名称、搜索零命中或静态工具报告认定无人使用。
-2. 优先调查重复表达同一事实的 Core/Skill/Adapter 判断、无人直接使用的字段或状态、已被当前定义完整替代的副本、只保护未使用 API 的守卫，以及不改变下一步的模板、Hash、兼容层和校验器。
-3. 对每个候选说明当前成本、删除或折叠后的净减少、保留的行为、失去的能力、受影响的使用方、验证方式和翻转条件。只把复杂度移动到包装层、脚本或新的定义位置不构成简化。
-4. 如果生产使用方、持久或兼容义务、已确认的产品决定仍然成立，则将其列为需要用户决定的产品候选；现有证据不足以推翻保留理由时，拒绝该候选。
-5. 只有代码、配置、schema、持久化或线格式、文档、测试和兼容行为都不再使用旧内容，且保留理由已经迁入当前定义时，才算完整替代；否则保留并说明部分替代关系。
-
-## 输出
-
-- 按“可直接清理 / 需要产品决定 / 保留 / 证据不足”分类，说明定义位置、实际使用方、证据、净删除、风险和最小验证范围。
-- 报告已调查范围与刻意排除范围；少量强候选优先于大量薄弱猜测。
-
-## 停止与禁止边界
-
-- 本 Skill 只读，不修改、删除、重构、发版或创建新的产品机制；用户明确要求实施后，按普通开发范围重新读取当前定义和授权。
-- 不因对称、形式统一、文件长度或“更完整”提出新抽象；不把测试或历史文档的存在自动当作生产需求。
-- 候选会改变入口、节点、职责、授权、持久数据或验收时停止清理结论，明确交给用户决定。
+给出少量有证据的“可清理、需要决定、保留、证据不足”候选，说明定义位置、差异、风险和翻转条件。只读调查不直接删除、修改或发版；用户要求实施后，按已确认范围和当前定义继续，不新增审计状态、评分表或专用流程。
--- .agents/skills/sacha-runtime-scenario/SKILL.md
+++ .agents/skills/sacha-runtime-scenario/SKILL.md
@@ -3,33 +3,18 @@
 description: 用于运行、恢复或评估 SachaOrchestra 的真实运行场景并保存直接证据；不用于普通静态测试、源码校验或未获授权的安装和新任务创建。
 ---
 
-# Sacha Runtime 场景
+# Sacha 运行场景
 
-## 功能
+在已明确的目标和授权内运行、恢复或评估具名场景，取得同一源码或安装身份、同一运行环境和同次运行的直接证据。
 
-按照 [Runtime 场景合同](../../../tests/runtime-scenarios/README.md)在隔离目录中运行一个具名任务包，保存直接证据并独立评估，确保一次结论只来自同一份源码、同一 Runtime 和同一次运行。
+## 入口与执行
 
-## 输入与首查
+1. 从用户请求或已有任务取得场景、目标运行环境与所需证据；新场景资格和完整运行步骤以[场景说明](../../../tests/runtime-scenarios/README.md)为准，普通静态检查不用本技能。
+2. 核对目标适配器、实际来源、上下文和已有授权；只有新增安装、用户任务或其他外部前提未被授权时才提问。已有批准直接消费。
+3. 按场景说明准备隔离目录、分配执行与评估输入、启动并保存本次原生记录。执行者不读取 oracle、源包或开发顶层设计；独立评估者只使用规定的裁决与直接证据。连续消息按顺序交付，不提前泄漏后续输入。
+4. 场景规定的执行者与独立评估者读取各自输入，具体隔离和调用步骤不在本技能复制。失败后继续同次运行时保留标识与轨迹；重新运行使用新隔离目录，不拼接不同运行的结果。
+5. 按 pass、drift 或 blocked 报告直接判断及第一处偏移；source-scenario 不冒充安装后发现，缺原生证据不以自报补齐。
 
-1. 要求一个现有场景包，或者用户已确认的新场景目标、目标 Runtime 和需要取得的证据；普通静态检查不使用本 Skill。
-2. 完整读取 Runtime 场景合同、目标 `task.md` 和测试夹具（`fixture/`）；执行者不得读取 `oracle.md`，独立评估者不得接收实现者的隐藏预期。
-3. 核对当前源码或已安装插件来源、目标 Adapter、运行授权和任务上下文。需要用户创建全新主任务、安装、外部账号、设备或其他高影响前置时先取得明确授权。
+## 结果与边界
 
-## 动作顺序
-
-1. 在工作区 `.temp/runtime-scenarios/<run-id>/<case-id>/` 创建唯一隔离目录，复制测试夹具、将任务改为中性名称并注入场景合同要求的工作区规则；不在源任务包内运行。
-2. 按任务包验收选择不携带父历史的委派 Agent，或使用用户授权的全新主任务。执行者只取得中性任务、隔离目录、目标 Runtime 正式入口和必需规则，不取得包名、`oracle.md`、开发顶层设计或结论提示。
-3. 每次运行单独保存准确的源码或安装身份、Runtime/Adapter、首次原生调用、返回标识、工作区改动、验证器原始输出、用户问答和必要的任务关系。失败重跑使用新的隔离目录，不拼接不同运行的轨迹或结果。
-4. 执行结束后创建未参与实现的独立评估者，只提供 `oracle.md`、目标 Adapter、直接记录、最终工作区和验证器输出；按 `pass | drift | blocked` 判断第一处偏移及其证据。
-5. 任务创建、安装后发现、Runtime 调用和外部状态分别报告。未安装的源码运行只能标记 `source-scenario`；缺少必要原生记录时判 `blocked`，不以自报补齐。
-
-## 输出
-
-- 返回场景、Runtime、源码或安装身份、隔离目录、直接证据、验证结果、独立结论和尚未验证的部分。
-- 保留仍供复核的本次证据；只清理本任务创建且已确认不再需要的临时文件。
-
-## 停止与禁止边界
-
-- 不预读或向执行者泄露 `oracle.md`，不用测试夹具字符串、校验器、静态源码或执行者总结替代 Runtime 行为。
-- 不自行安装插件、创建全新用户任务、修改用户配置、调用外部服务或扩大场景授权。
-- 新场景按 Runtime 场景合同的明确案例条件选择；仅有流程变化、角色或参数尚未覆盖不足以新增，不展开所有可能性。保留的包也不默认全部运行。
+交付场景、来源、运行环境、隔离目录、直接证据、验证与独立结论，保留供复核的内容。任务创建、源码、安装后发现、运行行为和外部状态分别说明；不自行安装、修改用户配置、创建新用户任务或扩大外部动作。场景只是当前问题的验证，不默认运行所有保留包或扩展角色、型号和状态组合。
--- PLUGIN_DESIGN.md
+++ PLUGIN_DESIGN.md
@@ -176,10 +176,7 @@
 | Executor | 在明确目标或批准 Scope 内实施、验证并交付真实变更/证据 | 核对 Scope/授权 → 主任务做必要 Manager 协调，或委派 Agent 返回协调请求 → 实施/集成 → 风险对应验证 → Review/收尾 | 冻结新方案、跨单元协调、独立 Review、项目文档 owner |
 | Reviewer | 以独立来源对照 Scope、Baseline、实现和原始证据裁决 | 核对 Gate/来源独立性 → 建立 Baseline → 复用有效证据，仅重跑能改变裁决的验证 → Outcome → 必要时重新 Review | 参与方案/实现、默认修复、创造新 Outcome/旁路 |
 
-Role Skill 必须自包含本行职责、局部流程和边界。修改 Skill 前先判断 delta：
-
-- 只改变职责内 procedure、证据读取方式或表达，可直接修改该 Skill 及唯一 Core/Adapter owner。
-- 新增职责、输出类型、跨节点路线、Human 决定点或接管其他 owner，属于顶层设计变化；先改本文并取得需要的 Human 批准，再改 Runtime 合同和 Skill。
+角色技能必须自包含本表声明的职责、局部流程和边界。维护时按[项目开发规则](AGENTS.md#开发改动与决定)区分职责内调整与高层设计变化。
 
 ## 5. 支持、控制与工具 Skill 能力设计
 
@@ -197,15 +194,10 @@
 | 工具/配置 | setup-project | 生成或刷新 Project Integration、Skill loading、Spec/Roadmap/项目文档存储 | 显式 project root/policy/path → 解析 provider/Skill → dry-run delta → 无未决变化时以当前 delta 写入 → 原子验证/回滚 | 主流程外；只写批准项目配置，不创建 Roadmap 或执行项目任务，不配置用户 Agent |
 | 工具/配置 | setup-agents | 创建、更新或核对 Sacha-owned Codex Agent definitions | 显式目标 → 解析 creator/runtime → dry-run → namespaced 原子写入/补偿验证 | 主流程外；只管理 Sacha-owned 用户配置，不派发 Agent，也不证明 Runtime discovery |
 
-支持/控制 Skill 的迭代可修改已声明功能内的局部做法。新增功能、入口、外部副作用或跨节点接管，先改本文；不能以“只是补一步”绕过顶层设计。
-
-## 6. 自上而下的变更顺序
-
-1. 写清需求不变量、真实 failure mode、授权和验收；判断是否改变顶层设计。
-2. 若改变入口、节点、连线、Role/Skill 职责或特殊流程，先修改本文并取得适用的 Human 批准。
-3. 按第 1 节确定唯一 Core owner：流程连线变化先修改 Workflow Contract；跨节点 Human 可见交互修改 Human Interaction Contract；其他局部判断只修改受影响的 Intake、Assurance、Coordination 或 Artifact owner。
-4. 修改直接消费该判断的 Role/支持 Skill；只在职责内完善 procedure。
-5. 修改受影响 Runtime Adapter、metadata/manifest；长期或 breaking boundary 变化才更新 Evolution。
-6. 核对本文与运行时负责文件及实际使用方；按明确案例和本次交付声明选择所需真实场景，不为覆盖节点、型号或参数展开矩阵。场景执行者不读取本文，独立评估者才用本文判断偏差。
-
-没有顶层变化时，不为“同步”触碰本文；发现下游需要新增图外路线或职责时，停止下游补丁并返回第 1 步。
+支持与控制技能只实现本表声明的功能；新增功能、入口、外部副作用或跨节点接管按[项目开发规则](AGENTS.md#开发改动与决定)处理。
+
+## 6. 开发维护入口
+
+变更批准和修改顺序按[开发改动与决定](AGENTS.md#开发改动与决定)处理，验证选择按[工具与验证](AGENTS.md#工具与验证)处理。本文的第 1 节提供负责位置，第 2～5 节定义现行产品范围和流程；只有这些内容变化时才修改相应设计。
+
+真实场景的执行者不读取本文；独立评估者可用本文核对产品范围和流程偏差。场景输入与证据隔离以[场景说明](tests/runtime-scenarios/README.md)为准。
--- docs/release.md
+++ docs/release.md
@@ -1,66 +1,49 @@
 # Sacha Orchestra 发版与安装指南
 
-> 文档身份：插件开发使用；不进入发布插件。仅在 Human 显式要求快速发版、普通发版、安装、重装或 cache parity 验收时读取。
+> 本指南只供插件开发使用，不进入发布插件。用于明确的发版、安装请求，以及本流程的维护。
 
-## Owner 与授权
+## 默认发版
 
-- [`EVOLUTION.md`](../EVOLUTION.md) 拥有当前 release、当前待发布源码版本、breaking boundary、成熟度和尚未实施的长期方向；三个 deployment manifest 拥有当前源码版本，Git annotated tag 标识已发布版本。
-- [`scripts/release.py`](../scripts/release.py) 只执行现有 Owner 已决定的机械步骤；失败后停止，不替代版本决定、Review、commit/tag/push 授权、安装授权或 Runtime 验收。
-- 发布执行者保护发布范围外的 working/untracked 内容，不执行 stash、reset、clean、rebase、强制 checkout 或历史改写。
+“发版”和“快速发版”使用同一路径：交付已经完成的改动，复用仍有效的测试和评审；只对新增未验证差异、失效证据或已知问题补验。发布动作本身不触发重新开发、全量 Review、回归、构建、安装或运行场景。
 
-## 快速发版
+- EVOLUTION.md 保存发布版本、待发布范围与破坏性边界；三个部署清单保存源码版本，annotated tag 标识已发布版本。README 只在入口、用法或其现有信息确实变化时更新，不为每次发版补一份总结。
+- 明确发版请求授权当前确定范围的 commit、annotated tag 和 push；不授权安装、刷新缓存、市场注册、外部服务修改或 Runtime 验收。用户给出版本时使用该版本；未给出时沿用 patch 递增默认，存在未决定的破坏性版本边界时只澄清该决定。
+- 保护范围外 working/untracked 内容，不执行 stash、reset、clean、rebase、强制 checkout 或历史改写。发布范围或目标不唯一时，先核实能自行确认的事实。
 
-Human 说“快速发版”时默认递增 patch 版本；只人工核对 Evolution 的当前 release 与当前待发布源码版本状态，并机器核对三个 deployment manifest、annotated tag 到 `HEAD` 的指向及 push 后远端分支/tag。普通回归、Skill/Plugin validator、完整 release coherence、安装/cache parity、fresh discovery 和 Runtime 不属于快速发版默认动作；只有根 `AGENTS.md` 的验证规则实际触发某项，或 Human 明确要求时才执行。
+## 准备与已有证据
 
-快速发版授权 commit、annotated tag 和 push，不授权安装、refresh、cache 修改或 Runtime 验收。未触发的检查不形成待补验证，也不要求在交付中列成欠账；已经出现的失败、明确风险或已触发但未完成的验收仍必须报告。
+1. 从当前任务、已批准 Spec、实施报告和明确提交确定待发范围，读取必要的原始测试结果与独立评审结论。核对它们仍适用于实际发布内容、验收输入和环境；不重新恢复完整实施过程。
+2. 已验证内容、验收输入和证据边界未变时直接复用。版本号、发布状态和纯发布说明变化只核对其准确性，不因 staged tree 改变就重跑测试或新增独立评审。
+3. 有新增未验证的行为差异或失效证据时，只补对应检查；原评审后的变化确实影响裁决时，只评审该差异与直接影响。已有明确失败或必需检查缺失不能当作“快速”理由跳过。真实场景是否需要仍按根 AGENTS.md 的触发条件判断。
+4. 更新必要的版本与发布信息，精确暂存本次发布内容。需要新增评审与测试时，两者在输入独立且无冲突的前提下并行；共享同一输入的命令结果直接复用，失败或范围变化只处理受影响部分。
 
-## 普通发版
-
-Human 说“发版”时，按实际改动运行必要的机械检查和最窄测试；真实 Runtime 只在根 `AGENTS.md` 的触发条件成立或 Human 明确要求时执行。安装仍按明确授权与发布目标决定。
-
-- 发版动作本身不触发 fresh discovery 或 Runtime 验收；没有具体行为问题、高风险验收要求或 Human 明确要求时，不为补齐版本记录而新建或重跑 Runtime 场景。
-- Scope、版本和 Review 结论稳定时优先使用 `scripts/release.py prepare|publish|install`；脚本不可用时才使用下文的定向 fallback。
-- 执行者发现同一 Scope 已有仍有效的独立 Review，且精确暂存发布内容、验收输入和证据边界未超出该 Review 时复用原结论；发版本身不触发重审。任一项变化时只审原结论后的精确暂存变化及其影响，按风险选择最低充分模型与推理强度，不因发布动作默认提高强度。实施收尾本应完成但缺失的 Review 只补未审 staged delta，不得借发版重启完整调查。
-- 执行者先精确暂存当前待发布源码版本对应的发布内容并取得唯一 staged tree；需要增量 Review 时立即以该 tree、精确 diff、受影响 Owner/消费者和已有证据派发独立 Reviewer，同时在主任务运行 `release.py prepare`，不得等待一方结束后才启动另一方。
-- `prepare` 返回的同一 tree JSON 是 Reviewer 可复用的验证回执：Reviewer 仍在运行时由主任务立即补发；Reviewer 已终态时由主任务核对回执 tree 与 Review tree 一致且无失败，不新开 Review。除非 tree 不同、输出缺少退出状态/结果摘要/失败计数，或存在会改变 Outcome 的具体冲突，Reviewer 不得重跑其中命令，也不得为恢复背景重新读取完整根 `AGENTS.md`、`PLUGIN_DESIGN.md`、历史或无直接消费者 Owner。
-- `prepare` 与 Review 通过后，维护者才把 Evolution 从待发布状态切换为当前 release，并依次执行 commit、annotated tag、发布阶段一致性检查、原子 push 和远端核对。tag 建立前不得向 Human 宣称当前待发布源码版本已成为 release，发布授权不得写入原实施 Scope。
-- 实施收尾时若 Reviewer Gate 已有事实依据，当前 Owner 应完成必要 Review；发布阶段只核对精确暂存发布内容是否仍在该 Review 的 Scope、验收输入和证据边界内。发布脚本允许精确暂存发布范围外存在无关工作区改动，但 `--candidate-path` 指定文件暂存后又变化、存在冲突或 index 验证失败时必须停止。
-- `release.py publish` 返回 `status=pass` 且携带 commit、tree、branch、tag、remote 与 `remote_verified=true` 后，执行者直接消费这些机器结果，只再运行一次 `cprobe summary` 确认任务外工作区边界并交付；不得例行重复查询 manifest、Evolution、HEAD/tag、远端 branch/tag、cache parity 或完整 plugin list。只有脚本缺少上述字段、结果冲突/失败、Human 明确要求安装或当前 Scope 包含 fresh Runtime 验收时才定向补查。
-
-普通发版先对当前待发布源码版本的精确暂存内容运行；`prepare` 从 Git index 导出隔离快照，验证不读取精确暂存发布范围外的 working/untracked 内容：
+`release.py prepare` 是按需补验工具，不是发版必经步骤。只有本次候选确实需要它选择的检查时才运行；仅需个别已有测试时，使用现有 staged_snapshot 导出的同一暂存树隔离快照执行相应命令并保留 tree 与原始结果，不用包含范围外改动的工作区结果替代候选证据，也不为了调用 prepare 重跑已经有效的检查。
 
 ```powershell
 python -B scripts/release.py prepare --version <version> --candidate-path <path> [--candidate-path <path> ...]
 ```
 
-复用仍有效的 Review 或完成必要的增量 Review 后，维护者把 Evolution 从待发布状态切换为当前 release 并精确暂存；再运行：
+prepare 按暂存文件选择检查并导出隔离快照，它不自动复用过去测试结果。DSH 路径会触发配套包验证及 pnpm verify；主插件发版不自动等于独立 DSH 包的重新构建或安装，只有对应交付确需补证且前置满足时才执行。缺少依赖时不得借验证自动安装。
+
+## 提交与发布
+
+必要准备已完成后，使用现有发布入口：
 
 ```powershell
-python -B scripts/release.py publish --version <version> --review reused|accepted --message <commit-message> --candidate-path <path> [--candidate-path <path> ...]
+python -B scripts/release.py publish --version <version> --review reused|accepted|not-required --message <commit-message> --candidate-path <path> [--candidate-path <path> ...]
 ```
 
-脚本不可用时，待发布与发布两个机器阶段分别运行 metadata coherence：
+- `--candidate-path` 必须精确匹配暂存文件；冲突、范围不一致或这些文件暂存后又变化时停止受影响发布。review 参数显式记录 reused（复用已有评审）、accepted（本次评审通过）或 not-required（按现行规则无需正式评审）；不能把无需评审写成已通过，也不能用参数替代实际证据。
+- 执行前核对版本与发布信息，按已确定版本更新 EVOLUTION.md 并暂存；tag 成功前不宣称版本已经发布。脚本执行 candidate 机器身份检查、commit、annotated tag、release 身份检查、原子 push 和远端核对，不运行回归测试或独立 Review。
+- publish 返回 status=pass，并给出 commit、tree、branch、tag、remote 和 remote_verified=true 后直接交付。只再用一次 cprobe 检查范围外工作区边界；不例行重查 manifest、HEAD/tag、远端或插件列表。
+- 失败时根据原始错误与已完成阶段恢复，不盲目重跑产生 commit、tag 或 push 的动作。脚本不可用时只对缺少的机械步骤使用原生 Git 与现有身份校验入口，不重新执行全流程。
+- 报告实际发布版本、提交和远端结果，以及明确失败、风险和未满足的必需条件。没有触发的安装或运行检查不形成欠账；耗时使用现有脚本输出，不凭感觉承诺固定总时长。
 
-```powershell
-python -B tests/validate_release_coherence.py --version <version> --phase candidate
-python -B tests/validate_release_coherence.py --version <version> --phase release
-```
+## 安装与缓存核对
 
-`candidate`（待发布阶段）只核对当前待发布源码版本的机器可解析部署身份和生产入口；`release`（发布阶段）在 commit、annotated tag 已建立且 Evolution 已人工切换为当前 release 后运行，并额外核对 annotated tag 精确指向 `HEAD`。该脚本不读取 README、Core、Adapter、Skill 或 Evolution 的说明文字。
+安装、重装、刷新、市场注册或 cache parity 需要对应明确授权；来源直连和版本变化本身不触发安装。
 
-## 安装与 cache parity
-
-- Marketplace 注册、安装、refresh、removal/reinstall 需要 Human 明确授权；实施或发布批准不隐含安装授权。
-- `codex plugin list` 显示已启用插件的 `PATH` 直接指向当前 repo-local plugin `root` 时，视为源码直连加载；版本变化本身不触发安装或 refresh。需要安装/cache 证据时先核对当前 `PATH`、生效版本和 source/cache parity，只有明确安装授权才调用安装 CLI。
-- 使用 `read_marketplace_name.py` 从 `.agents/plugins/marketplace.json` 读取 marketplace 名称；不得根据目录名猜测。
-- 授权后按目标 Adapter 执行并验证 marketplace/plugin list；Scope、版本、目标、branch/remote 未变化时不重复询问。
-- 安装前关闭本次发布创建且已终态的辅助 Agent；安装返回拒绝访问或 cache 已创建但登记未完成时停止并报告，不删除、覆盖或手改 cache，待占用解除后再用同一 CLI 恢复并核对。
-- manifest 使用批准的精确 semantic version，不加 cachebuster；不得编辑 cache、应用权限或系统 PATH。
-
-只有 Human 明确要求安装、重装或 cache parity 验收时运行：
-
-```powershell
-python -B scripts/release.py install --version <version>
-```
-
-安装证据只覆盖目标 Runtime 的实际安装、发现和 source/cache parity；源码校验、发布成功或 repo-local 直连不能替代安装后 fresh discovery 与 Runtime 行为。
\ No newline at end of file
+1. 需要安装证据时核对当前插件 PATH、版本和目标；PATH 指向仓库发布 root 表示源码直连，不等于新安装或运行通过。市场名称使用当前 read_marketplace_name.py 读取，不根据目录名猜测。
+2. 目标、版本与授权明确后，按适配器或现有 `release.py install --version <version>` 执行。相同决定不重复询问，不改安装缓存、系统 PATH 或应用权限，不添加 cachebuster。
+3. 安装前关闭本次发布创建且已经终态、可关闭的辅助代理。拒绝访问或缓存已创建但登记未完成时保留状态与错误；解除占用后用同一 CLI 恢复，不手工删除或覆盖缓存。
+4. 安装结果只证明对应安装、发现和来源/缓存一致性；实际行为需要相应运行证据。发布成功不能代替安装后的发现和运行结果。
--- scripts/release.py
+++ scripts/release.py
@@ -561,7 +561,7 @@
     prepare_parser.add_argument("--candidate-path", action="append", required=True)
     publish_parser = commands.add_parser("publish")
     publish_parser.add_argument("--version", required=True)
-    publish_parser.add_argument("--review", choices=("reused", "accepted"), required=True)
+    publish_parser.add_argument("--review", choices=("reused", "accepted", "not-required"), required=True)
     publish_parser.add_argument("--message", required=True)
     publish_parser.add_argument("--remote", default="origin")
     publish_parser.add_argument("--candidate-path", action="append", required=True)
--- tests/test_release.py
+++ tests/test_release.py
@@ -484,6 +484,30 @@
             remote_head = self.git(root, "ls-remote", "origin", "refs/heads/main").stdout.split()[0]
             remote_tag = self.git(root, "ls-remote", "origin", "refs/tags/v0.1.0^{}").stdout.split()[0]
             self.assertEqual((head, tag, remote_head, remote_tag), (head, head, head, head))
+
+    def test_publish_cli_records_review_not_required(self) -> None:
+        with tempfile.TemporaryDirectory() as root_dir, tempfile.TemporaryDirectory() as remote_dir:
+            root = Path(root_dir)
+            remote = Path(remote_dir)
+            self.init_release_repo(root, remote)
+            entry = root / "scripts" / "release.py"
+            entry.parent.mkdir()
+            entry.write_bytes(SCRIPT.read_bytes())
+            result = subprocess.run(
+                [release.current_python(), "-B", str(entry), "publish",
+                 "--version", "0.1.0", "--review", "not-required",
+                 "--message", "release", "--candidate-path", "candidate.txt"],
+                cwd=root, check=True, capture_output=True, text=True, encoding="utf-8",
+            )
+            payload = json.loads(result.stdout)
+            self.assertEqual(payload["status"], "pass")
+            self.assertEqual(payload["review"], "not-required")
+            self.assertIs(payload["remote_verified"], True)
+            head = self.git(root, "rev-parse", "HEAD").stdout.strip()
+            tag = self.git(root, "rev-parse", "v0.1.0^{}").stdout.strip()
+            remote_head = self.git(root, "ls-remote", "origin", "refs/heads/main").stdout.split()[0]
+            remote_tag = self.git(root, "ls-remote", "origin", "refs/tags/v0.1.0^{}").stdout.split()[0]
+            self.assertEqual((payload["commit"], tag, remote_head, remote_tag), (head, head, head, head))
 
     def test_publish_preserves_unrelated_working_change(self) -> None:
         with tempfile.TemporaryDirectory() as root_dir, tempfile.TemporaryDirectory() as remote_dir:
