# 三阶段规则整理：决定与调查记录

> 用途：供本次需求澄清、后续实施规格和恢复使用；不是现行规则或实施批准。

## 已确认范围

- 2026-09-05，用户接受由 Sacha 编排本次三阶段整理。
- 第一阶段：全局 AGENTS.md 与个人技能，重新判断跨项目表达、工作方法和限制的必要性。
- 第二阶段：Sacha 项目开发规则、开发技能和脚本维护要求，检查可读性、重复规定、过重的决策与验证步骤。
- 第三阶段：Sacha 运行流程，检查入口、澄清、research、grill、决定记录、Spec、执行、评审和收口存档；Gate、Hash、重复确认和其他防御机制均需核对实际使用方与失败保护。
- 用户特别要求核实 playwright、cli-creator 的官方来源、安装原因、更新和可能的本地自然化历史，再判断是否需要恢复原版。
- 按阶段澄清和形成规格；后续阶段不提前冻结全部方案。第一阶段五文件变化后来已获批准并实施，记录见 execution-report.md；未授权恢复或更新官方技能、后续阶段产品行为修改、安装或发布。中途要求的仓库冲突与单次本地提交已另行完成。

## 第一阶段已核实事实

- 两个技能均存在于 OpenAI 官方仓库 openai/skills 的 skills/.curated 下；它们属于官方可选技能，不因来源是 OpenAI 就等于 .system 预装技能。
- 当前本地安装器的 install-skill-from-github.py 第 290～291 行将默认安装目录设为 $CODEX_HOME/skills；未设置 CODEX_HOME 时使用 ~/.codex。该行为解释目录选择，不证明本机实际安装事件。
- 2026-09-05，通过 git ls-remote 取得上游 main 提交 49f948faa9258a0c61caceaf225e179651397431，再读取该提交归档并在内存中逐文件比较。
- playwright：本地与上游均为 9 个文件，文件集合一致；2 个文件原字节相同，7 个文件仅有换行或 BOM 差异；无内容差异。
- cli-creator：本地与上游均为 4 个文件，文件集合一致；4 个文件仅有换行或 BOM 差异；无内容差异。
- 本次比较没有写入、安装或执行两个技能的脚本。当前没有内容更新或恢复原版的必要；此结论不证明技能运行行为，也不能排除过去改写后又恢复的历史。
- 本地 playwright 目录创建时间为 2026-08-04T08:56:20Z，cli-creator 为 2026-07-16T03:23:16Z；文件创建时间不能证明首次安装时间或安装者。按这两个日期有界查询会话中的安装、复制、整理和恢复相关记录，未找到直接安装证据，历史原因仍未确认。
- 前次调查提出的 Bash、安装和固定 CLI 步骤确实来自上游原版，不应归为本地为了限制模型新增的规则。是否需要环境适配应与自然化或恢复原版分开判断。

上游基线：<https://github.com/openai/skills/tree/49f948faa9258a0c61caceaf225e179651397431/skills/.curated>。

## 第一阶段精简尺度

- 用户已明确选择“允许按实际风险和读者需要调整”，保留现有授权、用户改动保护和证据底线。
- 可调整每次固定统计文件数或行数、纠正后的固定步骤及无实际用途的逐项检查。具体行为变化在第一阶段规格和差异中明示，不称为语义等价改写。
- playwright、cli-creator 当前内容与上游一致，本阶段保持不变；不为自然中文输出维护官方技能的本地译写分支。
- 用户已批准第一阶段 Spec，随后要求将“审阅 AGENTS.md 和 Skills 的不清晰、冲突与重叠指令，重点检查自主性、澄清、批准、任务完成，消除重复确认、提前停止、过度工程或无法完成任务的规则，以 GPT-6 Astra 为优化目标”加入第一阶段要求；已写入 Spec 的目标与验收。
- 第一阶段已按五文件范围实施；原提案的澄清条件补入无法自行确认的必要任务信息，独立静态复核由 Needs Fix 转为 Accepted。具体结果与未验证边界见 execution-report.md。

## 仓库冲突与本地提交

- 用户在规划过程中要求先解决冲突并提交一批，再继续。冲突涉及 AGENTS.md 与 tests/runtime-scenarios/README.md。
- 合并保留已进入 HEAD 的真实运行触发条件，以及待提交 Astra 修改中的安全解析、场景精简、连续消息隔离与实际使用方核对。
- 本地提交为 d292056b975c6a05200aa4ca70d275fb2504892a，包含 132 文件；未推送。本任务三阶段记录与提案未纳入该提交。
- 提交前本轮直接执行 Python 120 项测试、DSH 6 文件 36 项定向测试、插件结构及 Explore 元数据校验，退出均为 0；最终 staged diff 空白检查退出 0。提交后跟踪文件与索引干净，未解决冲突为 0，其他未跟踪内容保留。

## 后续调查边界

- 第二、三阶段分别核对开发维护约束与生产流程，不将同名 Gate、Hash 或长文本直接判为冗余。
- 每个保留或精简候选都需说明具体负责文件、实际使用方、保护的失败、现有替代，以及删除后失去的行为。生产中的并发原像检查与仅供展示的指纹分别判断。
- 工作区已有未发布 Astra 修改；本次不覆盖或重做。源码已经删除的固定三遍回读与固定两个代理要求，不再作为待办。
- 本次执行读取的是已安装 0.14.2；源码候选不自动成为本次的新执行规则。安装包仍保留部分源码已去掉的要求，源码与安装后行为分别验证。
- 第一阶段的精简尺度与 Spec 已获批准，规格见 spec.md，拟议差异见 stage-1-proposed.patch；全局写入限于批准的五文件变化。后续阶段的产品路线、授权策略和机器接口变化分别给出具体取舍。

## 后续阶段的已定位候选

- 开发规则：AGENTS.md 第 69 行仍将“无法完整验证”纳入三个 Gate，需与同文件第 70～71 行的交付层及非阻塞后续处理统一；本次合并已收窄真实场景触发条件，不再把旧无条件场景要求列为待办。
- 开发规则：高层变化批准与设计优先顺序在根规则、变更表、顶层设计及开发技能重复；需折叠重复定义，保留实际产品决定与同步。
- 运行脚本：setup-project 的 _prepare_temp 对刚写入字节与内存原文分别计算 SHA，当前两边均可直接读取，可评估改为字节比较；不影响其他原像与补偿检查。
- 运行脚本：setup-project 在确定 AGENTS 是否真正变化前要求 expected_agents_sha256，可能给 no_changes 增加无用途前提；需用真实入口验证后再确定修改。
- 运行脚本：document-project 在一次生成与写入过程中核对整个 profiles.json 的 hash，无关 Profile 或排版变化可能扩大冲突范围；尚未复现，不能声称独立 dry-run 与 write 调用之间也由同一 hash 保护。
- 运行脚本：兼容旧 SOURCE SHA-256 标记及现有输出 hash 的外部使用方尚未完整确认，不作为直接删除项。
- 上述 Hash 候选均未实施；原像、临近写入复查和失败补偿继续保留。后续按实际使用方、机器接口及授权影响确定范围。

## 第二阶段规划

- 用户明确选择在本任务继续阶段二；当前只准备需求与规格，不修改尚未批准的项目规则。
- 七文件提案包括根 AGENTS.md、docs/AGENTS.md、四个开发技能和 PLUGIN_DESIGN.md 的开发维护段。负责文件表、产品图与能力表、术语索引和运行场景说明保留。
- 主要调整：证据缺口不自动进入产品 Gate；开发批准按真实变化消费；文档与评审不无条件检查远端或插件结构；固定全字段检查改为按受影响语义核对；参数与脚本按实际使用需要决定。
- 已核对本次目标范围当前无差异或冲突。阶段一全局规则和 d292056 合并后的验证触发作为现状输入；新阶段规格见 stage-2/spec.md，具体差异见 stage-2-proposed.patch。
- 本阶段拟保持七文件纯正文范围，不改变任何发布插件、生产脚本、元数据、部署接口或运行场景。实际 Gate 与 Hash 仍留在阶段三。
- 提案已完成原文匹配、差异结构、28 个本地引用和产品图/表不变核对；独立静态审阅发现的三项问题已修正，结果为 Accepted。阶段二 Spec 待用户批准，七个目标文件仍未写入。
- 用户随后补充发版耗时需求，并明确选择“普通发版默认复用已有测试和评审，只补新增差异；快速发版保留为同一路径兼容说法”。阶段二新增 docs/release.md，沿用 publish 的轻量交付动作，不新增回执缓存或 Hash 机制。
- 增量审阅发现现有必填 --review 无法记录无需正式评审的情况；修订将它增加 not-required，并新增真实 CLI 的本地隔离发布用例。阶段二最终拟改十文件；个别补验仍绑定同一暂存树的隔离快照。新增参数是开发发布接口补全，不改变产品 Gate 或取消必要评审。
- 扩展后的十文件后来已获用户整体批准并完成实施。发布脚本 32 项测试通过，十文件差异与空白检查通过，独立复核 Accepted；见 stage-2/execution-report.md。本次未正式提交、发版、网络推送或安装；测试及静态检查不证明实际发版耗时。

## 第三阶段规划

- 用户明确使用 Sacha Planner 在当前项目目录的新任务继续；已有 Sacha 接受不重问。本阶段只形成 Spec 与具体差异，未授权修改产品、提交、推送、发版或安装。
- 2026-09-06，用户选择“实施批准包含状态收口”：完成且必需证据满足后，可原位将本次唯一已批准 Spec 标记为已完成；项目文档仍按原有策略授权。此为提案的产品决定，不等于批准实施本阶段。
- 当前发布插件目录无差异或冲突；阶段二未提交修改和无关内容保留。规划执行使用已安装 0.14.2，修改对象为当前源码；已安装的固定回读要求不重列为源码待办。
- 当前源码核对支持收窄三个 Hash 候选，但尚未运行复现：临时文件字节比较；AGENTS unchanged 时免除缺失 expected 的强制检查，显式 stale expected 仍拒绝；模板目录比较选中 Profile、全局 generation_policy 和模板字节指纹，继续二次完整目录校验。
- 保留旧 SOURCE SHA-256 的兼容解析、公开输出 sha256、计划确认、目标原像复查、原子提交和失败补偿。
- 第三阶段 [Spec](stage-3/spec.md) 与[拟议差异](stage-3/proposed.patch)列出其余明确调整：Explore 起始意图按缺口选择、Spec 来源按用途区分、先完成可完成的补证、复用有效评审检查、按审查价值展示细节、消费已有具体文档确认。未修改目标产品文件。
- 提案自检修正了阻塞文档未完成前自动收口的问题；独立复核要求将两种文档阻塞性分支加入验收，补齐后返回 Accepted，见 [阶段三复核](stage-3/review.md)。该结果只覆盖待批准静态方案，尚未批准或执行实施。
- 用户随后要求将来源任务交接的 Token 优化统一并入第三阶段。增量评审复用已在提案覆盖；新增仅为 Coordination 的当前决定/消费者约束、父任务避免重走研究，以及 Workflow 既有 Spec 审阅时及时推荐迁移。范围增至 14 个候选产品/设计文件，两个测试文件仍只按既定验收补充。未扩展 Spec 前迁移、模型路由、统计机制或任何自动新任务。
- 全局 AGENTS.md 与 features.context_management.experimental_mode 由来源任务负责，本任务不修改；[用量报告](usage-report.md)作为固定开发诊断引用，不重新统计、不作账单或节省比例推断。
- 用户随后明确“批准spec”。第三阶段已实施：14 文件批准产品/设计差异、两个测试文件及既有场景体系内的一个高风险验收包和索引；44 项生成器测试通过，独立源码复核 Accepted，同次 Astra/medium source-scenario 独立评估 pass。详见 [实施记录](stage-3/execution-report.md)。没有提交、推送、发版或安装；按已确认授权原位完成本次 Spec 状态。
