# 本会话模型与 token 用量

统计截至 2026-09-06T00:57:47.082000+08:00，覆盖主任务与 6 个直接子代理，不包括新建的阶段三用户任务。使用原始 token_usage_record，按 response_id 去重，并核对累加值与线程累计计数一致；不把 token_count 的多次累计快照重复相加。

| 模型与强度 | 请求数 | 非缓存输入 | 缓存输入 | 输出 |
| --- | ---: | ---: | ---: | ---: |
| gpt-6-astra/medium | 161 | 1,738,729 | 49,102,848 | 89,652 |
| gpt-5.6-luna/max | 51 | 366,599 | 6,261,248 | 55,090 |
| gpt-5.6-sol/medium | 74 | 475,208 | 7,315,840 | 30,209 |
| 合计 | 286 | 2,580,536 | 62,679,936 | 174,951 |

累计总 token 为 65,435,423。缓存输入占输入的 96.05%。推理输出 86,872 已包含在输出中；缓存输入已包含在原始 input_tokens 中，均不能重复相加。这是多次请求的累计用量，不是单次上下文大小、独立新增文本量或订阅账单金额。

阶段二结束时的固定小计为 60,850,425 token（2026-09-06 00:34:30 +08:00）。其后本次统计与新任务交接截至上述时间又产生 4,584,998 token，计 10 个主模型请求；它同样受当前大上下文影响，不算阶段三执行成本。

## 子代理明细（截至阶段二结束）

| 单元 | 请求数 | 总 token（含缓存输入） | 输出 |
| --- | ---: | ---: | ---: |
| /root/development_constraints | 14 | 1,052,866 | 20,410 |
| /root/runtime_hash_constraints | 37 | 5,630,071 | 34,680 |
| /root/commit_scope_review | 28 | 3,507,352 | 6,910 |
| /root/stage1_review | 9 | 509,781 | 2,670 |
| /root/stage2_proposal_review | 20 | 2,157,556 | 13,532 |
| /root/stage2_implementation_review | 17 | 1,646,568 | 7,097 |

## 可节省环节

- 阶段二结束前主任务已有 151 个模型请求，单次输入从 46,888 增至 449,392。阶段结束后在新任务用当前决定和最小事实接续，能避免反复携带整个历史；此前建议阶段二留在同一任务，在成本上不够理想。
- 主任务文件读取工具返回约 480,048 字符，exec 返回约 295,177 字符（含网页和命令结果，不能直接换算 token）。根 AGENTS.md 被读取 5 次；其中有版本变化的必要回读，也存在可改为局部差异核对的空间。网页公共导航、完整旧规格和重复规范应更早截取到当前决定所需范围。
- 来源调查、冲突提交与第一阶段提案混在一段长执行中；发版需求又增加了多轮提案和补丁修复。后期每次机械修正都带着约 40 万输入。相互独立的确定性修正应批量处理，控制工具结果长度，避免为了补一个字段连续发多次模型请求。
- runtime_hash_constraints 单元使用 Luna Max，37 个请求、5,630,071 总 token、34,680 输出，其中推理 28,605。它提前调查了阶段三且读取范围偏广；应延后到阶段三，先读具体调用点与失败前提，不以较便宜模型为由扩大调查。
- 独立评审共 4 个 Sol 单元；阶段一、阶段二评审实际发现了澄清遗漏、隔离要求和发布参数缺口，不应全部删除。最可压缩的是对已有评审实现的提交前再次广泛调查（commit_scope_review 为 3,507,352 token）；优先核对新增冲突差异和有效证据。
- 当前执行使用的安装版 0.14.2 仍含源码已去掉的固定回读/派发要求；源码修改并不自动更新安装后流程。本轮没有借成本优化自行安装或换模型。

上述节省方向来自本次轨迹，未做A/B实验，不给出未经测量的节省比例或美元估算。原始分模型、分线程、分回合数字与对应日志路径见 usage-audit.json。
