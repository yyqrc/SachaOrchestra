# Sacha Orchestra 0.1.0 Foundation Bootstrap Execution Report

## Execution identity

- Task ID: `SO-0.1.0-BOOTSTRAP-2026-07-12`
- Execution Role: Executor
- Start time: `2026-07-12T12:26:00+08:00`
- Executor closeout time: `2026-07-12T14:12:47+08:00`
- Workspace root: `C:\Users\<user>\Documents\MarketPlace\SachaOrchestra`
- Scope authority: `spec.md`
- Architecture guardrail: `docs/architecture/evolution.md` (read-only)
- Workspace authorization: approved by the Human Conductor in the current task
- External installation authorization: granted by the Human Conductor with `授权执行 Slice 7`; executed at `2026-07-12T13:47:04+08:00`

## Planner Artifact baseline

| Artifact | SHA-256 |
| --- | --- |
| `spec.md` | `369038E224BBC4BA6DB43E64523417F42199E86FF596376BAFABAB91694B585F` |
| `docs/architecture/evolution.md` | `A5996976C6C84E3A25BF699B5EA977A3317967B37B24B1CEA53D368EC2214027` |

Both artifacts were read in full before workspace writes. The approval supplied in the current task supersedes the stale "draft, awaiting approval" status text in the frozen Spec; the Spec itself was not edited.

## Slice status

| Slice | Status | Evidence summary |
| --- | --- | --- |
| 0 - startup and tool preflight | Passed | Baseline inventory, rules, creator references, CLI, marketplace identity, and isolated validator Python verified |
| 1 - marketplace and plugin scaffold | Passed | Official helper created and JSON assertions verified the repo-local marketplace and plugin baseline |
| 2 - Framework Core | Passed | Two version-1 normative documents created with distinct ownership and no forbidden platform/project term hits |
| 3 - Codex Runtime Adapter | Passed | Codex-only mapping created; references Core without copying its contracts and gates all external state changes |
| 4 - Role Skills and alias | Passed | Four Skills initialized and generated by official tooling; final metadata and explicit-only alias policy verified |
| 5 - Project AGENTS, README, metadata | Passed | Single root Project AGENTS, non-normative README, and minimal 0.1.0 manifest metadata completed and checked |
| 6 - static and official validation | Passed | Four Skill validators, plugin validator, metadata, references, boundaries, hashes, and full delivery-tree checks passed |
| 7 - marketplace registration and plugin installation | Passed | Explicit authorization recorded; repo-local marketplace and plugin source/status/version verified through current CLI |
| 8 - fresh-context discovery and smoke | Passed | Fresh explicit and implicit Role routing, forward repair loop, alias behavior, SH1 read-only entry, and fixture cleanup verified |
| 9 - closeout and Reviewer handoff | Passed | Final validators, static checks, installation state, tree, cleanup, hashes, and formal Reviewer handoff completed |

## Slice 0 - startup and tool preflight

### Starting workspace snapshot

Initial files:

```text
spec.md
docs/architecture/evolution.md
```

No source-unknown or conflicting files were present. The workspace and its ancestors were not a Git repository (`git rev-parse --show-toplevel` exit `128`); Git was not initialized.

Effective instruction discovery found no workspace or ancestor `AGENTS.md` / `AGENTS.override.md`. The effective global file was read completely:

- `C:\Users\<user>\.codex\AGENTS.md`
- SHA-256: `5777F15BDD86662B1333F902C63A36C07A28DF4C657D3C0A9F8DE721D7FAAE54`

### Creator and validator sources

The following local Skills and required references were read in full before scaffold work:

- `C:\Users\<user>\.codex\skills\.system\plugin-creator\SKILL.md`
- `C:\Users\<user>\.codex\skills\.system\plugin-creator\references\plugin-json-spec.md`
- `C:\Users\<user>\.codex\skills\.system\plugin-creator\references\installing-and-updating.md`
- `C:\Users\<user>\.codex\skills\.system\skill-creator\SKILL.md`
- `C:\Users\<user>\.codex\skills\.system\skill-creator\references\openai_yaml.md`

Resolved script roots:

- plugin creator: `C:\Users\<user>\.codex\skills\.system\plugin-creator`
- skill creator: `C:\Users\<user>\.codex\skills\.system\skill-creator`

The current `--help` output confirmed the Spec-required arguments for `create_basic_plugin.py`, `read_marketplace_name.py`, `update_plugin_cachebuster.py`, `init_skill.py`, and `generate_openai_yaml.py`. `validate_plugin.py --help` and `quick_validate.py --help` initially exited `1` because their module-level `yaml` import was unavailable; this was a validator prerequisite failure, not a schema result.

### Validator Python

Checks against the system Python and bundled workspace Python both failed with `ModuleNotFoundError: No module named 'yaml'` (exit `1`). In accordance with the Spec, a workspace-local environment was created at `.temp/validator-venv`:

```powershell
& C:\Users\<user>\AppData\Local\Programs\Python\Python313\python.exe -m venv .temp\validator-venv
& .\.temp\validator-venv\Scripts\python.exe -m pip install --disable-pip-version-check PyYAML
& .\.temp\validator-venv\Scripts\python.exe -c "import sys, yaml; print(sys.executable); print(yaml.__version__)"
```

Results:

- venv creation exit: `0`
- PyYAML install exit: `0`
- import check exit: `0`
- Python: `3.13.14`
- PyYAML: `6.0.3`
- errors: `0` after installation
- warnings: `0`
- failures: `0`

No global Python package or workspace-external configuration was changed.

During Slice 0, the Human Conductor separately installed PyYAML `6.0.3` into the current user's Python and reported a successful import. That workspace-external user action was not run by the Executor and was not used as validation evidence; this execution continued with the isolated `.temp/validator-venv` environment.

### Codex CLI and marketplace state

`CODEX_CLI_PATH` was not exported into the shell. `Get-Command codex` resolved the Windows Store application entry, and direct execution failed with access denied. The current Codex config was queried only for its `CODEX_CLI_PATH` entry; the resolved executable was verified without changing config.

Commands and results:

```powershell
& <codex-cli> --version
& <codex-cli> plugin --help
& <codex-cli> plugin marketplace --help
& <codex-cli> plugin marketplace list
& <codex-cli> plugin list
```

- CLI version: `codex-cli 0.144.0-alpha.4`
- all five verified CLI commands exited `0`
- configured marketplace names: `openai-primary-runtime`, `openai-bundled`, `openai-curated`
- `personal` marketplace identity: absent
- installed or available `sacha-orchestra` plugin: absent
- default user marketplace file `C:\Users\<user>\.agents\plugins\marketplace.json`: absent
- errors in verified CLI commands: `0`
- warnings in verified CLI commands: `0`
- failures in verified CLI commands: `0`

The failed Windows Store entry was not modified. No marketplace was registered and no plugin was installed, removed, or refreshed.

### Slice 0 verification decision

The workspace path, frozen Artifact hashes, effective rules, creator and validator entrypoints, executable Codex CLI, available marketplace identity, and isolated validator environment are verified. No workspace-external state was changed by the Executor. No creator/schema conflict requiring a Planner return was found. Slice 0 passed.

## Slice 1 - marketplace and plugin scaffold

Immediately before scaffold, `plugin marketplace list` exited `0` and still listed only `openai-primary-runtime`, `openai-bundled`, and `openai-curated`; neither the `personal` identity nor the default user marketplace file existed. The target plugin and repo-local marketplace paths were also absent.

The verified helper command was run without `--marketplace-name`, `--category`, or `--force`:

```powershell
& .\.temp\validator-venv\Scripts\python.exe `
  C:\Users\<user>\.codex\skills\.system\plugin-creator\scripts\create_basic_plugin.py `
  sacha-orchestra `
  --path C:\Users\<user>\Documents\MarketPlace\SachaOrchestra\plugins `
  --with-skills `
  --with-marketplace `
  --marketplace-path C:\Users\<user>\Documents\MarketPlace\SachaOrchestra\.agents\plugins\marketplace.json `
  --install-policy AVAILABLE `
  --auth-policy ON_INSTALL
```

Scaffold exit: `0`. The helper created `.codex-plugin/plugin.json`, an empty `skills/` parent for the later official Skill initializers, and `.agents/plugins/marketplace.json`. It did not create hooks, MCP, apps, assets, scripts, templates, or other optional components.

Both generated JSON files were read completely. `read_marketplace_name.py --marketplace-path ...` returned `personal` with exit `0`. PowerShell JSON parsing then verified all of the following with assertion exit `0`:

- marketplace `name`: `personal`
- marketplace `interface.displayName`: `Personal`
- exactly one `sacha-orchestra` entry
- source: `local`, path `./plugins/sacha-orchestra`
- policy: `AVAILABLE` / `ON_INSTALL`
- marketplace entry category: `Productivity`
- plugin directory and manifest name: `sacha-orchestra`
- scaffold manifest base version: `0.1.0`

Errors: `0`; warnings: `0`; failed assertions: `0`. Slice 1 passed.

## Slice 2 - Framework Core

The three legacy Role Skills were consulted read-only only for cross-project Scope, evidence, verification, and handoff discipline. Project-specific paths, models, commands, release rules, and control mechanisms were not migrated.

Created:

- `plugins/sacha-orchestra/core/workflow-contract.md`
- `plugins/sacha-orchestra/core/artifact-protocol.md`

Both files declare `Contract Version: 1` and product version `0.1.0 Foundation`. Ownership is non-overlapping: the Workflow Contract owns the three production Roles, Optional Manager boundary, three Gates, lifecycle, escalation, and repair routing; the Artifact Protocol owns progressive Artifact semantics, authority boundaries, and the nine-field Handoff Envelope.

The files were read completely after creation. Directed assertions verified all three Role sections, Optional Manager, all three Gates, both contract-version markers, all nine exact Handoff fields, and that `Author` occurs only once in the migration note. A case-insensitive Core scan for platform, execution-instance, engine-selection, interface-state, internal-storage, and named-project terms produced no matches (`rg` no-match exit `1`, interpreted as pass). Assertion command exit: `0`; errors: `0`; warnings: `0`; failed assertions: `0`.

No later-stage integration, Manager implementation, registry, work-packet schema, parallel execution facility, or second Adapter was created. Slice 2 passed.

## Slice 3 - Codex Runtime Adapter

Before writing, current CLI help for `plugin marketplace add`, `plugin add`, `plugin remove`, and `plugin marketplace remove` was read; all four commands exited `0`. This established the supported parameter syntax without changing installation state.

Created `plugins/sacha-orchestra/adapters/codex/runtime-adapter.md`. It maps independent Role contexts, Skill discovery, Artifact reachability, recovery, model and budget selection, the single-writer rule, repo-local marketplace operations, and fresh-context verification to Codex. Future Manager/subagent/parallel support is documented only as a constrained Adapter boundary; no implementation, registry, work-unit schema, or capability claim was added.

The Adapter was read completely after creation. Directed assertions verified both Core references resolve, the nine-field contract is not copied, named project terms and stable runtime-instance identifiers are absent, the external authorization gate is explicit, the adapter version remains `0.1.0`, and no Manager or parallel implementation is claimed. Assertion exit: `0`; errors: `0`; warnings: `0`; failed assertions: `0`. Slice 3 passed.

## Slice 4 - Role Skills and compatibility alias

The official `init_skill.py` initialized `planner`, `executor`, `reviewer`, and `spec-author` without resources or examples. All four initializer commands exited `0`. Generated TODO bodies were then completely replaced with concise Role workflows that directly reference both Core documents; the three formal Skills reference the Adapter only for platform operations.

The official `generate_openai_yaml.py` was run after the final Skill bodies for all four Skills with explicit `display_name`, 25-64 character `short_description`, and a `default_prompt` containing the matching `$skill-name`. All four generator commands exited `0`. After the final generation, `spec-author/agents/openai.yaml` received the required `policy.allow_implicit_invocation: false`; no generator was run after that policy update.

All eight files were read completely. Directed checks verified:

- the only Skill directories are `planner`, `executor`, `reviewer`, and `spec-author`;
- every Skill contains exactly `SKILL.md` and `agents/openai.yaml`;
- YAML frontmatter contains only matching `name` and `description` keys;
- all Core and required Adapter references resolve;
- each default prompt names its Skill explicitly;
- the three formal Skills omit policy and therefore retain the current default implicit discovery;
- the deprecated alias is explicit-only and forwards to Planner;
- no initializer placeholder marker, resource directory, Skill README, Manager, `spec-executor`, or `spec-reviewer` exists.

YAML parse/assertion exit: `0`; errors: `0`; warnings: `0`; failed assertions: `0`. Slice 4 passed.

## Slice 5 - Project AGENTS, README, and metadata

Created the single workspace-root `AGENTS.md`. It records the repo-local marketplace and single-plugin project facts, owners and direct entrypoints, task-based read routing, Core/Adapter/Skill maintenance boundaries, current scaffold and validator commands, installation authorization gate, non-Git baseline, and `.temp` ownership rules. It explicitly preserves all Global safety, authorization, evidence, and engineering requirements and routes long-term or breaking-change work to `docs/architecture/evolution.md` without copying Core contracts.

Created the plugin-root `README.md` as a non-normative entrypoint. It links to Core and Adapter authorities, names the three formal namespaced Role entries expected after installation and the deprecated alias, identifies `0.1.0` as the Foundation seed, distinguishes Contract Version 1 from product `1.0.0`, and explicitly states the absent future capabilities and read-only self-hosting limit.

Updated only scaffold-supported fields in `.codex-plugin/plugin.json`: cross-project framework description, `Sacha Orchestra` author/developer identity, dynamic Role-routing descriptions, `Productivity`, capabilities `Interactive` and `Write`, and one short starter prompt. The base version remains exactly `0.1.0`; the manifest declares only the existing `./skills/` component.

All three files were read completely. JSON parsing and directed assertions verified every required manifest field, component existence, and absence of unsupported component fields. Workspace enumeration found exactly one `AGENTS.md`, at the root. Every Markdown link in the plugin Core, Adapter, README, and Skills resolved from its owning file. Assertion exit: `0`; errors: `0`; warnings: `0`; failed assertions: `0`. Slice 5 passed.

## Slice 6 - static and official validation

### Official Skill validators

The following commands were run separately with the isolated validator Python:

```powershell
& .\.temp\validator-venv\Scripts\python.exe C:\Users\<user>\.codex\skills\.system\skill-creator\scripts\quick_validate.py C:\Users\<user>\Documents\MarketPlace\SachaOrchestra\plugins\sacha-orchestra\skills\planner
& .\.temp\validator-venv\Scripts\python.exe C:\Users\<user>\.codex\skills\.system\skill-creator\scripts\quick_validate.py C:\Users\<user>\Documents\MarketPlace\SachaOrchestra\plugins\sacha-orchestra\skills\executor
& .\.temp\validator-venv\Scripts\python.exe C:\Users\<user>\.codex\skills\.system\skill-creator\scripts\quick_validate.py C:\Users\<user>\Documents\MarketPlace\SachaOrchestra\plugins\sacha-orchestra\skills\reviewer
& .\.temp\validator-venv\Scripts\python.exe C:\Users\<user>\.codex\skills\.system\skill-creator\scripts\quick_validate.py C:\Users\<user>\Documents\MarketPlace\SachaOrchestra\plugins\sacha-orchestra\skills\spec-author
```

Each printed `Skill is valid!` and exited `0`. Validator errors: `0`; warnings: `0`; failures: `0` of `4`.

### Official plugin validator

```powershell
& .\.temp\validator-venv\Scripts\python.exe C:\Users\<user>\.codex\skills\.system\plugin-creator\scripts\validate_plugin.py C:\Users\<user>\Documents\MarketPlace\SachaOrchestra\plugins\sacha-orchestra
```

Output: `Plugin validation passed: ...\plugins\sacha-orchestra`. Exit: `0`; errors: `0`; warnings: `0`; failures: `0`.

### Metadata and marketplace assertions

Both JSON files and all eight YAML/frontmatter documents were parsed using the isolated Python plus PyYAML. Sixty-eight directed assertions passed with `0` failures, covering:

- marketplace name/display, exact single entry, local source, source path, policies, and entry category;
- manifest key set, name, exact version `0.1.0`, description, author, actual `./skills/` path, interface key set, `Productivity`, and exact `Interactive` / `Write` capabilities;
- exact four-Skill directory set, frontmatter key/name agreement, generated interface key set, 25-64 character short descriptions, and matching `$skill-name` prompts;
- default implicit discovery for the three formal Skills and exact `false` alias policy;
- required Core, Adapter, and Planner-relative references.

`read_marketplace_name.py --marketplace-path .agents/plugins/marketplace.json` was rerun and returned `personal` with exit `0`. A fresh read-only `plugin marketplace list` exited `0` and confirmed `personal` is still not registered or occupied.

### Delivery-tree and leakage checks

The complete non-temporary delivery tree was compared to the Spec's exact allowlist. Results after the final rerun:

- delivery files: `18`
- missing files: `0`
- extra files: `0`
- empty delivery directories: `0`
- forbidden paths or extra state files: `0`
- initializer placeholder or scaffold-default hits: `0`
- machine-local absolute path hits inside the plugin: `0`
- named project-rule hits inside the plugin: `0`
- platform, execution-instance, engine-selection, interface-state, storage, or named-project leakage hits in Core: `0`

The first placeholder scan exited `1` as a validation failure because this report quoted the literal placeholder marker while describing its absence; no product file contained a placeholder. The report wording was corrected, and the entire tree/leakage command was rerun with exit `0` and failed checks `0`.

Future-capability term hits were read individually. They are limited to the normative Optional Manager and Manager Gate definitions required by Core, explicit absence statements in README/Adapter, or the Adapter's non-implementing future mapping boundary. No Manager, Router, Registry, work-packet, parallel-execution, project-integration, or later-stage implementation path exists; future-capability path hits: `0`.

### Markdown, reference, and authority checks

All `12` Markdown files were decoded and read. Code fences were balanced, every document had one leading H1, no heading level jumped, and all local Markdown links resolved. The Project AGENTS references to both Core documents, the Adapter, and Evolution resolve from workspace root.

Workspace enumeration found exactly one `AGENTS.md` and no plugin-root duplicate. Directed authority checks confirmed root AGENTS and README do not reproduce Role sections or the nine Handoff fields, Global requirements are not relaxed, long-term work routes to Evolution plus a separately approved Spec, the non-Git fact is recorded, and installation and temporary-file gates are present.

### Frozen Artifact and external-boundary checks

- `spec.md` SHA-256 remains `369038E224BBC4BA6DB43E64523417F42199E86FF596376BAFABAB91694B585F`.
- `docs/architecture/evolution.md` SHA-256 remains `A5996976C6C84E3A25BF699B5EA977A3317967B37B24B1CEA53D368EC2214027`.
- Global AGENTS SHA-256 remains its startup value `5777F15BDD86662B1333F902C63A36C07A28DF4C657D3C0A9F8DE721D7FAAE54` and its last-write time predates this execution.
- The three legacy Role Skill files read from RenderDocAnalysis have last-write times predating this execution.
- Current plugin-creator and skill-creator roots contain the same file sets seen at startup; files modified at or after execution start: `0`.
- The default user marketplace file remains absent. The Executor has not registered a marketplace, installed or removed a plugin, edited cache, changed application permissions, or changed system PATH.

### Independent read-only audits

Two independent read-only audits corroborated the primary checks. The tree/schema audit reported `71/71` directed checks passed and no issue beyond the expected temporary venv and then-pending report closeout. The architecture audit found no Core/Adapter/Project boundary, Stage 1+, version, ownership, README/AGENTS, or alias issue; its only open item was this Slice 6 report update, which is now applied. Subagent reports are corroboration only and do not replace the primary evidence above.

### Validation claim separation

| Claim surface | Status | Evidence boundary |
| --- | --- | --- |
| Repository lint | Not configured / not run | No project lint runner exists; do not infer lint success |
| Static conformance | Passed | Tree, JSON/YAML, Markdown, links, authority, leakage, hashes, and forbidden-item checks exited `0` after the documented rerun |
| Skill validators | Passed | Four independent `quick_validate.py` commands exited `0` |
| Plugin validator | Passed | `validate_plugin.py` exited `0` |
| Build | Not applicable | Stage 0 contains documents and Skill/plugin metadata, with no build target |
| Test suite | Not applicable | No project test runner exists; directed validators and assertions are reported separately |
| Marketplace registration / plugin installation | Not run - authorization gated | No workspace-external write was performed by the Executor |
| Installed plugin discovery | Not run | Requires verified installation |
| Fresh-context forward smoke | Not run | Requires verified installation |
| SH1 readiness smoke | Not run | Requires verified installation; when run, it can prove only the read/plan entry |

Slice 6 passed. The current state is **workspace-local complete / installation unverified**. Proceeding to Slice 7 requires a new, explicit Human Conductor authorization for both repo-local marketplace registration and plugin installation.

## Slice 6 delivery hashes

These are the SHA-256 values after Slice 6 for every non-temporary file except this still-mutable execution report. Its final self-hash will be computed after the later installation/smoke/closeout updates; embedding a current self-hash would immediately invalidate it.

| File | SHA-256 |
| --- | --- |
| `.agents/plugins/marketplace.json` | `3E4D249D1B95F4237B538C0CD252506BE870A7999A9A99C9663858BD9F7050C2` |
| `AGENTS.md` | `B7BC28EE91CB3D32387D11CAA21E913EF63E543D379758097C225695DFBA83BB` |
| `docs/architecture/evolution.md` | `A5996976C6C84E3A25BF699B5EA977A3317967B37B24B1CEA53D368EC2214027` |
| `plugins/sacha-orchestra/.codex-plugin/plugin.json` | `50ABD1024B4EFA25841D3C4D78FEA055968163C5C8042EFE3F5055ED7A742B27` |
| `plugins/sacha-orchestra/adapters/codex/runtime-adapter.md` | `EE6FCB99AC0A75A1FD1C24136BEBFFE757A3DA875178723749176A7C7143DF86` |
| `plugins/sacha-orchestra/core/artifact-protocol.md` | `5CAC4AEEC1AF4565E9B2B57E329CDF79ECCB84BCA0554C209A9DAFD8FCCE6946` |
| `plugins/sacha-orchestra/core/workflow-contract.md` | `88340925307CBCF49BF97D477149138DC3D80DEA414A1D05997779F868FFBFA5` |
| `plugins/sacha-orchestra/README.md` | `E7F3F3BB0FCD4C8B76590BD7566F3C44F002D2335619998416335376C7451E18` |
| `plugins/sacha-orchestra/skills/executor/agents/openai.yaml` | `E596B9E01CB9A4E1A5183AFD5E11A5C07F46DF0867EC386A2DAB084080C72709` |
| `plugins/sacha-orchestra/skills/executor/SKILL.md` | `FA5B8078327612BE99C82531CAA07049EC518FCF8A96150056601E329F806073` |
| `plugins/sacha-orchestra/skills/planner/agents/openai.yaml` | `B5D3C94EE675F345F8DF315C4DE83101B008CDDD48FBE6DD97DD55B1F79CD583` |
| `plugins/sacha-orchestra/skills/planner/SKILL.md` | `E011976E00A4D7233E04891ACAE9AF7D2FA4296FB4E291CEA76CBF4802E95F78` |
| `plugins/sacha-orchestra/skills/reviewer/agents/openai.yaml` | `809EB7B89C9AD6349A3162DEA76FBDB78DFDA5C5078A08228F8240CD96EFEE41` |
| `plugins/sacha-orchestra/skills/reviewer/SKILL.md` | `02021C2C1D28D16ED35F6D184D80163B1E586B5EA1A0714C9DFFE9BE6677FC98` |
| `plugins/sacha-orchestra/skills/spec-author/agents/openai.yaml` | `FC2CFE69B4256AC639CD62ADC347BEFBB9499275DC7C2D6014D0E7E36D61BA82` |
| `plugins/sacha-orchestra/skills/spec-author/SKILL.md` | `800C593362199572D8ACDA50CBE3A5E2E2AFF87056103C313709989C1FEC9E66` |
| `spec.md` | `369038E224BBC4BA6DB43E64523417F42199E86FF596376BAFABAB91694B585F` |

## Slice 7 - authorized marketplace registration and plugin installation

### Authorization and pre-install revalidation

The Human Conductor explicitly replied `授权执行 Slice 7`, authorizing the two workspace-external Codex state changes in this Slice. No cachebuster, uninstall, reinstall, cache edit, permission change, or PATH change was authorized or performed.

Before external writes, the Executor reread the effective Project AGENTS, current plugin-creator and skill-creator instructions, plugin installation reference, and Spec Slice 7-9. This turn then verified:

- frozen Spec and Evolution hashes still match the Slice 0 baseline;
- manifest version is exactly `0.1.0`;
- Slice 6 is recorded as passed;
- configured CLI exists and reports `codex-cli 0.144.0-alpha.4`;
- `plugin --help`, `plugin marketplace --help`, marketplace list, and plugin list all exit `0`;
- `personal` is not registered, the default user marketplace file is absent, and no `sacha-orchestra` plugin appears before install;
- helper-read marketplace name is `personal`; entry source, `AVAILABLE`, `ON_INSTALL`, and `Productivity` assertions pass;
- four Skill validators and the plugin validator all rerun with exit `0`, errors `0`, warnings `0`, and failures `0`.

### Registered marketplace

The executable CLI was resolved from the current config as `C:\Users\<user>\AppData\Local\OpenAI\Codex\bin\a7c12ebff69fb123\codex.exe`. The authorized command was:

```powershell
& <resolved-codex-cli> plugin marketplace add C:\Users\<user>\Documents\MarketPlace\SachaOrchestra
```

Exit: `0`. CLI output reported marketplace `personal` added from the workspace. A separate `plugin marketplace list` exited `0` and showed exactly one `personal` row with root `C:\Users\<user>\Documents\MarketPlace\SachaOrchestra`.

### Installed plugin

Immediately before installation, `read_marketplace_name.py --marketplace-path .agents/plugins/marketplace.json` again returned `personal` with exit `0`. The authorized command was:

```powershell
& <resolved-codex-cli> plugin add "sacha-orchestra@personal"
```

Exit: `0`. A separate `plugin list` exited `0` and showed exactly one matching row:

```text
sacha-orchestra@personal  installed, enabled  0.1.0  C:\Users\<user>\Documents\MarketPlace\SachaOrchestra\plugins\sacha-orchestra
```

The matching marketplace row still points to the workspace root. Status, marketplace identity, source path, and exact version assertions all passed. The Executor did not inspect or edit plugin cache. Slice 7 passed.

## Slice 8 - fresh-context discovery and smoke

### Fresh-context discovery

Fresh Codex contexts created after installation discovered the installed plugin without a reinstall or cachebuster. An explicit Planner context exposed all three formal Role entries (`sacha-orchestra:planner`, `sacha-orchestra:executor`, and `sacha-orchestra:reviewer`) and performed planning only; the neutral fixture remained empty until a separately authorized Executor context ran. No fresh context reported or treated the deprecated alias as a fourth production Role.

| Context purpose | Minimal input | Role hit | Output or Artifact | Verification result |
| --- | --- | --- | --- | --- |
| Planner forward | Explicit namespaced Planner; one exact neutral text file; planning only | `sacha-orchestra:planner` | Scope, Non-goals, acceptance, Planner to Executor Envelope; no file write | Formal catalog exposed all three Roles; fixture remained empty |
| Executor forward | Smoke Spec, Planner handoff, fixture root, and bounded Human authorization only | `sacha-orchestra:executor` | Only `planner-smoke.txt`; raw byte evidence; Executor to Reviewer Envelope | Initial contract contradiction reported, then verification-only continuation passed |
| Reviewer forward | Smoke Spec, evidence index, and real fixture only; default no fix | `sacha-orchestra:reviewer` | Independent judgment and Reviewer handoff; no Artifact file | Initial Reject to Planner, final Accept after contract repair; no fix performed |
| Implicit routing | Neutral planning-only prompt that did not name any Skill | `sacha-orchestra:planner` | Minimal Scope, Non-goals, and acceptance; no file write | Planner selected; alias not selected |
| Deprecated alias | Explicit `$sacha-orchestra:spec-author`; read-only compatibility question | `sacha-orchestra:spec-author`, forwarded immediately to Planner | Deprecation notice and minimal Planner output; no file write | Explicit path worked; alias reported `allow_implicit_invocation: false` |
| SH1 readiness | Explicit namespaced Reviewer; fixed SH1 Task ID; workspace read-only | `sacha-orchestra:reviewer` | `Accept with follow-up`, one future suggestion, Reviewer to Human Envelope; no Artifact file | Authority references resolved; only SH1 read entry claimed |

### Planner to Executor to Reviewer forward smoke and repair loop

The bounded neutral task used Task ID `SO-0.1.0-SMOKE-001` and the workspace-owned fixture root `.temp/sacha-orchestra-smoke/neutral-project`. Planner defined exactly one file, `planner-smoke.txt`, with exact ASCII content `Sacha Orchestra neutral smoke artifact.` and no trailing newline. Every Role transition used the exact nine Handoff fields in the prescribed order.

The first Planner turn incorrectly derived the literal length as `41` bytes even though the exact literal is `39` bytes. This was a task-level arithmetic defect in the smoke Spec, not a framework-source defect. The fresh Executor context created only the specified file, detected the contradiction during byte-level verification, reported exit `1`, errors `0`, warnings `1`, failure count `1`, and did not add unapproved bytes. The independent fresh Reviewer context reproduced `39/39` literal bytes, no trailing newline, and only the impossible `41`-byte condition; it rejected and routed the same Task ID to Planner rather than modifying the fixture or misrouting to Executor.

The first Reviewer probe itself exited `1` with one PowerShell generic-call tool error before producing valid acceptance counters. The Reviewer explicitly discarded that probe, changed only the byte-comparison mechanism, and reran. The authoritative initial Review result then had exit `1`, errors `0`, warnings `1`, failure count `1`, where the sole failure was the impossible `41`-byte contract condition. The invalid probe did not support or replace the judgment.

Under the same Task ID, Planner changed only the derived byte count from `41` to `39`; filename, exact content, Scope, and Non-goals were unchanged. The existing user authorization for this bounded smoke remained sufficient because the correction did not expand Scope. The same Executor context then performed verification only and reported exit `0`, errors `0`, warnings `0`, failure count `0`, exactly one ordinary file, `39/39` bytes, exact byte equality, no trailing newline, and SHA-256 `D529247409C6BABD4347D404C0A6AAF88A620420F99BD50C37DF53E1B05D8AF6`. The same independent Reviewer context reran the revised acceptance and returned `Accept` with the identical hash and zero failures.

The primary Executor independently repeated the final byte-level check in this context. It exited `0` with errors `0`, warnings `0`, failure count `0`, ordinary-file count `1`, expected/actual `39/39`, byte equality `True`, trailing newline `False`, and the same SHA-256. This repair loop demonstrates Planner correction, Executor verification-only continuation, Reviewer return routing, Task-ID continuity, and evidence preservation without a framework-source change.

The primary check read the fixture with `[System.IO.File]::ReadAllBytes`, compared Base64 encodings of actual and expected byte arrays, tested the final byte for CR/LF, counted recursive ordinary files, and computed `Get-FileHash -Algorithm SHA256`; it exited nonzero if any of those five assertions failed.

All formal transitions used this exact field order: `Task ID`, `Source Role`, `Target Role`, `Outcome`, `Scope Reference`, `Artifact References`, `Evidence References`, `Deviations and Open Risks`, `Entry Condition`. The observed route was Planner to Executor, Executor to Reviewer, Reviewer to Planner on the contract defect, Planner to Executor after the bounded correction, Executor to Reviewer after verification, and Reviewer to Human Conductor on Accept. No transition added, removed, renamed, or reordered a field.

### Implicit routing and compatibility alias

A separate fresh planning request deliberately named no Skill. It implicitly selected `sacha-orchestra:planner`, produced Scope, Non-goals, and acceptance criteria, and made no file change. It did not select `spec-author`.

Another fresh context explicitly invoked `$sacha-orchestra:spec-author`. It identified the entry as deprecated and explicit-only, confirmed `policy.allow_implicit_invocation: false`, immediately forwarded to the formal Planner workflow, did not model an additional Role, produced only a minimal plan, and made no file change. Together these runs verify both positive explicit alias compatibility and negative implicit alias routing.

### SH1 readiness read-only smoke

A fresh installed Reviewer context ran Task ID `SO-0.1.0-SELFHOST-READ-001`. It read the Project AGENTS, plugin manifest, both Core contracts, Codex Adapter, and Role entrypoints; resolved the required references; proposed exactly one bounded future improvement; performed no write; and returned `Accept with follow-up` to the Human Conductor using the exact nine-field Handoff Envelope. The sole future suggestion was to place a fixed read-only smoke input and minimal expected-result template beside the Adapter's fresh-context SH1 check under a separately approved future Planner Scope; it did not generate or implement that Scope.

Before the three read-only routing/SH1 contexts, the `18` non-temporary workspace files had aggregate SHA-256 `AABAE53FCC5A8E2F32885D3B0EB8D09E1832B2688F06510F81F6BD855FCF0126`. The same file count and aggregate hash were recomputed after all three contexts, proving the inspected file contents were unchanged. A separate directory audit found that fresh-context setup had temporarily created empty root `.git` and `.codex` directories at `2026-07-12T13:48:26+08:00`. Each had zero recursive children, and `git rev-parse --show-toplevel` still exited `128`; no repository was initialized. After confirming their shared task-time provenance and emptiness, the Executor removed only those two directories and verified both absent with exit `0`, errors `0`, warnings `0`, and failure count `0`.

This smoke proves only that the installed Foundation can enter a read-only self-inspection path from a fresh context. It does not prove SH2, SH3, bounded self-modification, automatic orchestration, complete self-hosting, or product `1.0.0`.

After all smoke evidence was recorded, the Executor resolved `.temp/sacha-orchestra-smoke` to the expected workspace-local absolute path, verified it contained only the single task-owned fixture file, removed that fixture tree, and confirmed removal with exit `0`, errors `0`, warnings `0`, and failure count `0`. The validator environment remained for Slice 9. Slice 8 passed.

## Created or modified files

- `execution-report.md` - created for the required progressive evidence record
- `.agents/plugins/marketplace.json` - created by the official plugin scaffold helper
- `plugins/sacha-orchestra/.codex-plugin/plugin.json` - created by the official plugin scaffold helper
- `plugins/sacha-orchestra/skills/` - created by the official plugin scaffold helper for Slice 4 initializers
- `plugins/sacha-orchestra/core/workflow-contract.md` - normative Role, Gate, lifecycle, and routing contract
- `plugins/sacha-orchestra/core/artifact-protocol.md` - normative Artifact and nine-field Handoff contract
- `plugins/sacha-orchestra/adapters/codex/runtime-adapter.md` - Codex-specific context, deployment, recovery, and verification mapping
- `plugins/sacha-orchestra/skills/planner/{SKILL.md,agents/openai.yaml}` - formal Planner Skill and generated metadata
- `plugins/sacha-orchestra/skills/executor/{SKILL.md,agents/openai.yaml}` - formal Executor Skill and generated metadata
- `plugins/sacha-orchestra/skills/reviewer/{SKILL.md,agents/openai.yaml}` - formal Reviewer Skill and generated metadata
- `plugins/sacha-orchestra/skills/spec-author/{SKILL.md,agents/openai.yaml}` - deprecated explicit-only compatibility alias and generated metadata
- `AGENTS.md` - sole Project AGENTS for source ownership, routing, commands, gates, and workspace facts
- `plugins/sacha-orchestra/README.md` - non-normative plugin entrypoint
- `plugins/sacha-orchestra/.codex-plugin/plugin.json` - scaffold manifest updated with required 0.1.0 metadata
- `.temp/validator-venv/**` - temporary validator environment; not a deliverable
- `.temp/sacha-orchestra-smoke/**` - task-owned forward-smoke fixture; created, verified, and removed
- `.temp/stage0_validate.py` - task-owned directed Slice 9 checker; temporary and not a deliverable
- root `.git/` and `.codex/` - empty fresh-context setup side effects; did not form a repository or contain files and were removed after ownership verification

## Deviations, open risks, and recovery entry

- Approved deviations: None.
- Observed and repaired smoke deviation: the initial Planner turn derived `41` bytes for an exact `39`-byte literal. Executor and Reviewer detected the impossible condition, Reviewer routed it to Planner, and Planner corrected only that derived value under the same Task ID. Final independent verification passed; no product source was changed for the repair.
- Validation-probe deviation: the first two runs of the temporary directed checker each exited `1` with errors `0`, warnings `0`, and four failures because the checker expected a Markdown H1 before Skill YAML frontmatter and then failed to account for the separator blank line. Only the task-owned checker was corrected. The third run executed the same `141` checks with exit `0` and failure count `0`; no product file was changed for this probe repair.
- Runtime side effect: fresh-context setup temporarily created empty `.git` and `.codex` directories. Both had zero children, Git remained uninitialized, and both were removed after task-time ownership and absolute targets were verified.
- Formal Review repair state: the first independent formal Review returned `Reject - Needs Evidence` solely because the report did not identify a reachable original location for the fresh-context runtime turns. It found no source, contract, metadata, validator, tree, hash, installation, or SH1-entry defect. The evidence-only locator below now exposes the existing raw task history without adding a Runtime storage ID; independent re-review remains pending.
- Version constraint: the product and manifest must remain `0.1.0`; no cachebuster is authorized in this execution.
- Recovery entry: an independent Reviewer can resume from the final Handoff below, read the frozen Spec, this evidence index, and the actual workspace, rerun the highest-value checks, and create only root `review.md` for its judgment.

## Slice 9 - closeout and Reviewer handoff

### Final validator and static evidence

The four official Skill validators were rerun separately after all plugin-source work and again after temporary-state cleanup. Each final command used Python `3.13.14` with PyYAML `6.0.3`, printed `Skill is valid!`, and exited `0`. Final Skill-validator errors: `0`; warnings: `0`; failures: `0` of `4`.

The official plugin validator was also rerun after cleanup, printed `Plugin validation passed`, and exited `0`. Plugin-validator errors: `0`; warnings: `0`; failures: `0`.

The corrected directed checker parsed both JSON files, all Skill frontmatter and generated YAML, enforced exact manifest and marketplace values, checked the four-Skill file sets and alias policy, resolved Markdown references, checked H1/fence/heading structure, enforced the exact delivery allowlist, checked the single root AGENTS, scanned for machine-local and project leakage, rejected forbidden Stage 1+ paths, verified both Contract Version markers, and recomputed both frozen Planner Artifact hashes. Final result: `141` checks, exit `0`, errors `0`, warnings `0`, failure count `0`.

Repository lint remains **not configured / not run** because the Stage 0 document-and-metadata workspace has no lint runner. Build and test suite remain **not applicable** because there is no build target or test runner. These statuses are not inferred from the validators.

### Final installation, discovery, smoke, and boundary state

`read_marketplace_name.py` returned `personal` with exit `0`. Final CLI checks separately returned one marketplace row rooted at this workspace and one plugin row with `sacha-orchestra@personal`, `installed, enabled`, exact version `0.1.0`, and the workspace plugin source path. Marketplace-list and plugin-list exits were both `0`; errors `0`; warnings `0`; failure count `0`.

Fresh-context discovery and forward smoke passed for Planner, Executor, and Reviewer; implicit Planner routing passed; the deprecated alias worked only when explicit; and the SH1 readiness check proved only the installed read/self-inspection entry. The initial smoke contract defect, repair route, and final independent acceptance are preserved in Slice 8 rather than hidden by the final pass.

Final external-boundary verification found the Global AGENTS hash unchanged at `5777F15BDD86662B1333F902C63A36C07A28DF4C657D3C0A9F8DE721D7FAAE54`, RenderDocAnalysis files modified since execution start `0`, plugin-creator and skill-creator files modified since execution start `0`, and default user marketplace file absent. `git rev-parse --show-toplevel` exited `128`. No manual cache edit, cachebuster, ACL, PATH, Global rule, system Skill, or other-project write was performed; the only external writes were the separately authorized `personal` registration and `sacha-orchestra@personal` installation.

### Temporary-state cleanup and final tree

The Executor resolved the task-owned `.temp` directory to its expected absolute path and verified its only remaining children were `validator-venv` and `stage0_validate.py`. It then removed that tree and confirmed `.temp` absent with exit `0`, errors `0`, warnings `0`, and failure count `0`. The earlier smoke fixture had already been removed separately after evidence capture.

The final pre-Review workspace contained exactly `18` files, one root `AGENTS.md`, zero empty directories, no `.temp`, no `.git`, no `.codex`, and no `review.md`. Missing allowlisted files: `0`; extra files: `0`; errors: `0`; warnings: `0`; failure count: `0`. The first independent formal Reviewer subsequently created the authorized nineteenth file, `review.md`, with a runtime-evidence-only Reject; it did not change any of the 18 Executor-closeout files.

### Final immutable-file SHA-256

All non-temporary files except this still-self-referential report were recomputed after cleanup. The values match their last verified source state:

| File | SHA-256 |
| --- | --- |
| `.agents/plugins/marketplace.json` | `3E4D249D1B95F4237B538C0CD252506BE870A7999A9A99C9663858BD9F7050C2` |
| `AGENTS.md` | `B7BC28EE91CB3D32387D11CAA21E913EF63E543D379758097C225695DFBA83BB` |
| `docs/architecture/evolution.md` | `A5996976C6C84E3A25BF699B5EA977A3317967B37B24B1CEA53D368EC2214027` |
| `plugins/sacha-orchestra/.codex-plugin/plugin.json` | `50ABD1024B4EFA25841D3C4D78FEA055968163C5C8042EFE3F5055ED7A742B27` |
| `plugins/sacha-orchestra/adapters/codex/runtime-adapter.md` | `EE6FCB99AC0A75A1FD1C24136BEBFFE757A3DA875178723749176A7C7143DF86` |
| `plugins/sacha-orchestra/core/artifact-protocol.md` | `5CAC4AEEC1AF4565E9B2B57E329CDF79ECCB84BCA0554C209A9DAFD8FCCE6946` |
| `plugins/sacha-orchestra/core/workflow-contract.md` | `88340925307CBCF49BF97D477149138DC3D80DEA414A1D05997779F868FFBFA5` |
| `plugins/sacha-orchestra/README.md` | `E7F3F3BB0FCD4C8B76590BD7566F3C44F002D2335619998416335376C7451E18` |
| `plugins/sacha-orchestra/skills/executor/agents/openai.yaml` | `E596B9E01CB9A4E1A5183AFD5E11A5C07F46DF0867EC386A2DAB084080C72709` |
| `plugins/sacha-orchestra/skills/executor/SKILL.md` | `FA5B8078327612BE99C82531CAA07049EC518FCF8A96150056601E329F806073` |
| `plugins/sacha-orchestra/skills/planner/agents/openai.yaml` | `B5D3C94EE675F345F8DF315C4DE83101B008CDDD48FBE6DD97DD55B1F79CD583` |
| `plugins/sacha-orchestra/skills/planner/SKILL.md` | `E011976E00A4D7233E04891ACAE9AF7D2FA4296FB4E291CEA76CBF4802E95F78` |
| `plugins/sacha-orchestra/skills/reviewer/agents/openai.yaml` | `809EB7B89C9AD6349A3162DEA76FBDB78DFDA5C5078A08228F8240CD96EFEE41` |
| `plugins/sacha-orchestra/skills/reviewer/SKILL.md` | `02021C2C1D28D16ED35F6D184D80163B1E586B5EA1A0714C9DFFE9BE6677FC98` |
| `plugins/sacha-orchestra/skills/spec-author/agents/openai.yaml` | `FC2CFE69B4256AC639CD62ADC347BEFBB9499275DC7C2D6014D0E7E36D61BA82` |
| `plugins/sacha-orchestra/skills/spec-author/SKILL.md` | `800C593362199572D8ACDA50CBE3A5E2E2AFF87056103C313709989C1FEC9E66` |
| `spec.md` | `369038E224BBC4BA6DB43E64523417F42199E86FF596376BAFABAB91694B585F` |

`execution-report.md` was the eighteenth pre-Review file. Its final SHA-256 must be computed after the last evidence-repair write and therefore is not embedded in itself; the independent Reviewer must recompute it from the actual file. Slice 9 passed at Executor closeout. The first formal Review reopened only the runtime-evidence index, not the implementation.

### Formal Review evidence-only repair and raw runtime locator

The first formal Reviewer independently passed the source, official validators, metadata, tree, frozen hashes, Core/Adapter boundary, exact version `0.1.0`, current marketplace registration, current plugin installation, installed Reviewer discovery, and read-only SH1 entry. It rejected Stage 0 solely because Slice 8 named no reachable original runtime location after cleaning the fixture.

No smoke rerun, plugin-source change, installation change, frozen-Artifact change, cache edit, or new evidence file was needed. The original raw prompts, Role-hit commentary, commands, outputs, validation counts, repair turns, and final answers remain reachable in the local Codex task history. A Reviewer can use the Codex task search on the local host with the search term below, select the exact human-visible task title, and read all turns. These titles are evidence locators, not Runtime storage IDs; no storage ID is embedded in this report.

| Evidence surface | Task-search term | Exact task title | Raw evidence to inspect |
| --- | --- | --- | --- |
| Planner discovery and forward, including correction | `smoke` | `Plan planner smoke` | Initial prompt, installed catalog hit for all three formal Roles, planning-only result, first nine-field Planner to Executor Envelope, Reviewer-return repair prompt, corrected 39-byte contract, second nine-field Envelope |
| Executor write and verification-only continuation | `smoke` | `Create smoke artifact` | Authorized bounded prompt, sole fixture write, raw byte-level command/result, detected 41/39 contradiction, first nine-field Executor to Reviewer Envelope, corrected-contract read-only verification, final zero-failure Envelope |
| Independent smoke Review and repair closeout | `smoke` | `Review smoke fixture` | Spec/evidence-only prompt, invalid generic-call probe disclosure, authoritative Reject to Planner, corrected-contract independent command/result/hash, final Accept and nine-field closeout |
| Implicit Planner positive and alias-negative check | `smoke` | `制定隐式路由烟测方案` | Prompt that names no Skill, actual implicit `sacha-orchestra:planner` hit, planning-only output, and absence of alias invocation |
| Deprecated alias explicit check | `alias` | `验证 spec-author alias` | Explicit `$sacha-orchestra:spec-author` prompt, deprecation notice, `allow_implicit_invocation: false`, immediate Planner forwarding, no-write output |
| SH1 readiness read-only smoke | `smoke` | `Review SH1 readiness` | Fixed SH1 Task ID prompt, installed Reviewer hit, authority reads, exact claim limit, one bounded future suggestion, no-write result, and nine-field Reviewer to Human Envelope |

The local task search was rerun during this repair. Search term `smoke` returned the five exact smoke/implicit/SH1 titles above, each idle and rooted at this workspace; search term `alias` returned exactly the alias task. Errors: `0`; warnings: `0`; failed locator assertions: `0`. Reading the listed raw turns allows the independent Reviewer to inspect the original context outputs without relying on the Executor summary or the deleted fixture alone.

This evidence-only repair remains inside the frozen Stage 0 Scope and the same bootstrap Task ID. It changes only the Executor-owned evidence index and preserves `review.md` as Reviewer-owned. The final handoff below supersedes the earlier evidence-location gap while preserving the first Reject as part of the audit trail.

## Executor to Reviewer Handoff Envelope

1. `Task ID`: `SO-0.1.0-BOOTSTRAP-2026-07-12`
2. `Source Role`: `Executor`
3. `Target Role`: `Reviewer`
4. `Outcome`: `Slices 0-9 of the approved 0.1.0 Foundation bootstrap are complete. After the first formal Review passed source and installation checks but rejected the missing runtime locator, the Executor added only human-readable local Codex task-search locators for the preserved raw fresh-context turns. No implementation, installation, frozen Artifact, or Review Artifact was changed; the same independent Reviewer can now reconstruct the required runtime evidence.`
5. `Scope Reference`: `spec.md` with SHA-256 `369038E224BBC4BA6DB43E64523417F42199E86FF596376BAFABAB91694B585F`; Stage 0 only. `docs/architecture/evolution.md` remained read-only with SHA-256 `A5996976C6C84E3A25BF699B5EA977A3317967B37B24B1CEA53D368EC2214027`.
6. `Artifact References`: `spec.md`; `execution-report.md`; `.agents/plugins/marketplace.json`; `AGENTS.md`; `plugins/sacha-orchestra/.codex-plugin/plugin.json`; `plugins/sacha-orchestra/core/`; `plugins/sacha-orchestra/adapters/codex/`; `plugins/sacha-orchestra/skills/`; `plugins/sacha-orchestra/README.md`.
7. `Evidence References`: `execution-report.md` Slice 6 official/static validation, Slice 7 authorized registration and installation, Slice 8 fresh-context and repair smoke, Slice 9 final validation/tree/hash evidence, and the Formal Review evidence-only repair table; actual workspace files, current CLI state, existing review.md, and the raw local Codex task turns located by the listed human-visible titles are independently reachable evidence.
8. `Deviations and Open Risks`: `No approved Scope or architecture deviation and no known implementation defect. The initial smoke Planner byte-count defect was repaired under the same Task ID; invalid probes and false-positive temporary checker runs were discarded and rerun; empty fresh-context setup directories were safely removed. The first formal Review Reject remains in review.md until Reviewer-owned re-review updates it. SH1 proves only read-only entry reachability, not SH2, SH3, full self-hosting, Stage 1+, or product 1.0.0.`
9. `Entry Condition`: `Continue the same independent sacha-orchestra:reviewer context. Read the updated report, use local Codex task search with the exact terms/titles in the evidence-only locator, inspect all raw turns rather than relying on the summary, independently decide whether the former runtime evidence gap is closed, and update only root review.md with the resulting judgment.`
